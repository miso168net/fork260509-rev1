# INTEGRATION-CHECKLIST — rev1 整合進度追蹤

> **此檔 = 整合進度的單一真相**：Current Focus（現狀）+ Follow-up Backlog（衍生工作）+ 已完成里程碑 + Roadmap 狀態 + 跨 feature 待驗證項。
> 不放原則（→ `.specify/memory/constitution.md`）、不放規格細節（→ `specs/<NNN>/`）、不放 brainstorm 決策（→ `docs/superpowers/<NNN>-feature.md`）、不放操作參考事實（如預設帳號 → CLAUDE.md §8）。
> 每次 session SOP hook（`.claude/hook-git-submodule-SOP.sh`）自動 cat 全檔注入 Claude session 第一輪 additional context —— 故各節須維持精簡。

---

## 🎯 Current Focus

**現狀**：DESIGN-A §6.1 全 14 application feature（F1–F14）+ Phase W deploy 全部 7 階段（W-F1~W-F7、W-F11、Track DESIGN-A W-FA1/2/3 + **W-F12/13/14 observability**）全部落地，**DESIGN-B（rust-only）形態生效**。**W-WEBUI 軌道 + W-WEBUI follow-up 軌道（W-FW5/6/7/8 + W-FW9）全部落地** —— 031–038 + 040 共九 feature。044/045/046/047/048/049 + **050 spec-hygiene-pass-4 落地** —— 050 = post-merge code review 衍生 5 Important + 1 Polish bundled mixed sprint：軌道內 TS-DepGraph-Hygiene 1 條 (049-R1 base-web Dockerfile strict isolation 紀律 comment polish、DESIGN-W-BASE-WEB §4.4.3 dynamic 行使首次) + 軌道外 5 條 (036-R1 sys_menu `Option<Option<T>>` double-option selective merge + 037-R1 Casbin `ptype='g'` defensive UPDATE + `g_rules_updated_count` audit + GeneralUser deny C-V + 044-R1 install prometheus pushgateway service v1.10.0 + cleanup binary recorder + scrape job + 044-R2 axum `MatchedPath` route template label + 046-R1 FR-015 generalized expansion budget amend wording + retro footnote)；0 Constitution amendment、0 base-web src/ diff、0 schema migration、0 new entity、0 new endpoint、0 new workspace cargo dep；C-V1~C-V10 全 PASS；連帶 R-row 038-R1 + 039-R1 Critical 留 dedicated sprint backlog。

**Active feature**：—（050 spec-hygiene-pass-4 已完成、見已完成里程碑）

**下一步**（user 2026-05-25 拍板 5 階段順序前 4 階段 (046/047/048/049/050) 全部交付完、剩 dedicated sprint 2 條 Critical + 條件觸發 + 長期 3 段）：
1. dedicated sprint（Critical、需排）：038-R1 audit/Casbin atomicity / 039-R1 wire shape leak
2. 條件觸發：042-N4 / 042-N5 / 048-N1 (d) 各自獨立（trigger driven）
3. 長期：F1.2 / W-F6b / W-F15/16 各自獨立

---

## 📋 Follow-up Backlog

> 各 feature 在 brainstorm / implement / acceptance / review 階段發現、需另開 feature 或 follow-up 處理的衍生工作項 —— 整合進度的「不遺漏」權威清單。新發現一律登記於此（per 維護指引）；排程成 feature 並完成後從本節移除（「已完成里程碑」會留紀錄）。

### 衍生 follow-up（feature 執行中發現）

| ID | 發現來源 | 項目 | 規劃去向 |
|---|---|---|---|
| 042-N4 | 042 C-V12 Part B | SC-005 stretch p50 ≤ 100ms 未達（p50=175ms with `drainer_sleep_interval_ms=50`、p95=188ms）；spec C-V12 預測 100-200ms 落點正常、p95 ≤ 500ms PASS | 可選優化：（a）middleware → drainer channel notify 替代 polling、（b）publish_audit_event inline in drainer per-row txn、（c）lower fetch txn overhead；觀測需求驅動觸發後再決定（044/045/046 落地後仍未撞為 bottleneck、留條件觸發）|
| 042-N5 | 042 C-V7 multi-replica | `docker compose -f docker-compose.dev.yml up --scale rust-api=2` 因 dev compose 將 host port 11081 bind 到單一 rust-api instance、scale 第二 instance 起來時 fail；無法在 dev stack 走真實多 replica，042 以單實例 50 並發代理驗 SKIP LOCKED + per-row idempotency 邏輯 | 改 dev compose 把 host port bind 改 profile/scaled-aware、或 W-F11 prod env load balancer 才能跑真多 replica；非 042 issue、屬 dev stack 配置缺口 |
| 048-N1 (d) | 048 / 049 follow-up | (a)+(b)+(c) 已於 049 結案：strict isolation + explicit devDeps + packageManager pin；剩 (d) 「下次 pnpm 升級重新檢視」；詳細歷史脈絡見下方 footnote | 下次 pnpm 升級（pnpm 12 或 newer corepack 強制 sha hash 等）觸發後 spec hygiene 軌道內 review |
| **038-R1** ⚠️ Critical | post-merge code review (2026-05-25) | FR-008 atomicity violation：`assign_permission` 的 audit txn 與 Casbin enforcer policy 寫入屬不同 txn；Casbin policy 寫成功後 audit 寫失敗無法 rollback、違反 Constitution II「audit 同 txn」承諾；當前 code comment 承認 "rare DB-error path" 但 spec 條款未軟化 | dedicated sprint：spec 補刪「unconditional atomicity」描述、或 pre-stage audit + commit alongside Casbin write（評估 enforcer API 的 atomicity 限制可行性）|
| **039-R1** ⚠️ Critical | post-merge code review (2026-05-25) | 3 endpoint 仍 leak raw entity Model `id: ULID-string`、wire shape inconsistency vs `SystemManageRoleOutput.id: i64`：(a) `GET /org` returns `PaginatedData<SysOrganizationModel>` (b) `POST /systemManage/addRole` returns `Res<SysRoleModel>` (c) `POST /systemManage/updateRole` 同 (d) `get_paginated_endpoints` returns raw `SysEndpointModel`；040 W-FW9 D 已修 sys_role/user/access_key 3 個、但漏 sys_organization + sys_endpoint + addRole/updateRole response | dedicated sprint：補 `SysOrganizationOutput` / `SysEndpointOutput` Detail wrap + role create/update handler return 改 `SystemManageRoleOutput`；對齊 040 體例（id=display_id i64、不含敏感欄位、Sea-ORM Model 不動） |
| 050-N1 | 050 implementer-stage (Batch 5 T025 acceptance) | cleanup binary 直接讀 `DATABASE_URL` env（main.rs:719），但 rust-api container 走 `APP_DATABASE_URL_FILE=/run/secrets/database_url` secret file 模式；`docker compose exec rust-api /usr/local/bin/cleanup` 預設 env 缺 `DATABASE_URL`、需手動 `-e DATABASE_URL=$(cat /run/secrets/database_url)` 才能跑；C-V4 acceptance 期間 user 手動傳 env 跑通、屬 dev/cron 配置缺口 | 改 cleanup binary 支援 `*_FILE` secret file pattern（讀 `APP_DATABASE_URL_FILE` / `PUSHGATEWAY_URL_FILE` 等）對齊 rust-api 主程序；或在 docker-compose secrets 階段把 `DATABASE_URL` 直接以 env var inject 給 cleanup execution。屬 044-R1 落地後 dev/cron parity 議題、非 spec 範圍 |
> **5 Important + 1 Polish 結案（050 spec-hygiene-pass-4 落地、2026-05-25）**：post-merge code review 衍生 R-row 6 條已於 050 sprint 結案：
> - 036-R1 FR-004 omitted 不覆寫 drift → impl fix selective merge（`Option<Option<T>>` double-option DTO + `update_menu_selective` service handler、保留 spec 036 FR-004 原 promise）
> - 037-R1 grouping rule 'g' 未 sync → defensive `UPDATE casbin_rule SET v1=$1 WHERE ptype='g' AND v1=$2` + audit payload 加 `g_rules_updated_count` 數值欄（per 050 Clarifications Q2 MUST） + GeneralUser deny C-V（code=5001 deny）
> - 044-R1 cleanup_job_rows_deleted_total silent no-op → install pushgateway service `prom/pushgateway:v1.10.0` + cleanup binary recorder `install_pushgateway_recorder()` + scrape job (`honor_labels: true`) + rustls CryptoProvider follow-up fix（reqwest pull rustls 0.23+ 需 explicit install）
> - 044-R2 route cardinality → axum `MatchedPath` template + fallback `"unmatched"`（3 numeric ID curl → 單一 `/systemManage/getRoleHome/{roleId}` series）
> - 046-R1 FR-015 expansion budget breach → generalized wording amend（base ≤3 + user 拍板可加大、commit msg body 紀錄）+ 046 retro footnote
> - 049-R1 Dockerfile stale comment → line 35-42 改 strict isolation 紀律說明（per 049 FR-004/005、packageManager field SoT + 4 phantom promoted + --ignore-scripts rationale）
>
> 040 W-FW9 dynamic 行使（DESIGN-W-WEBUI §7）後第二次 dynamic 行使：本次為 TS-DepGraph-Hygiene 軌道、DESIGN-W-BASE-WEB §4.4.3 首次落 sprint entry。
>
> **046 entry budget enlargement footnote（050 retro 補登）**：046 US4 implementer-stage expansion 實際 18 sites（17 display_id ActiveModel/Model field + 1 home_route_name）+ dev-dep `tokio "time"` feature flag 為 pre-existing E0063 build-gate fix、user 拍板加大；屬 046 spec FR-015 generalized amend 後（050 sprint 落地）的首次行使。

> **base-web TS `id` 型別債（已分階段完成）**：原 030–034 W-WEBUI 軌道遺留（`src/typings` 與 rust wire 真實型不一致）已分三階段結案：
> - **039 rust-entity-id-numeric-migration**（2026-05-24）：rust 端 5 業務 entity 加 `display_id BIGINT` 副欄 + Snowflake 53-bit generator + wire 邊界 transform，post-039 wire `id` 為 i64 number 而非 ULID string；`CommonRecord.id: number` 與 `Menu.parentId: number` 因此**自然對齊**（不需 TS 改）。
> - **040 wire-id-consistency**（2026-05-24）：base-web W-WEBUI 軌道修 service inline type + button-auth-modal / menu-auth-modal 內部 cascade；拆掉 032 留下的 `deserialize_i32_or_string` workaround；W-FW9 條目。
> - **048 base-typings-sync**（2026-05-25）：TS-Typing-Sync 軌道首發 sprint、修剩餘 3 處 mismatch（`MenuRoute.id` string→number、`MenuRoute` 加 `pid` 宣告、`MenuTree.pId` rename + retype 為 `pid: string`）+ 3 處 JSDoc audit（`CommonRecord.id` Snowflake / `Menu.parentId === 0` sentinel / `Auth.UserInfo.userId` ULID vs display_id）。原 inline note 中「`CommonRecord.id` / `parentId` 改 `string`」建議在 039 後實際是反方向（保留 number、補 JSDoc 說明）。
> **041-N1 status enum errata 結案**（044 落地後）：rust-api `status="1"/"2"` literal + base-web `status === '1'/'2'` 雙 grep 100% 0 hit、main code paths 既有 transform layer + 明文界定（per 044 Clarifications Q3）；後續 features 若撞 status enum 混雜模式再列 046+ spec hygiene-pass。
> **048-N1 pnpm 11 hoist env-repair（已結案 (a)+(b)+(c)、(d) trigger-driven 留）**：原 048 sprint 期間 host vs container pnpm version drift / pnpm 11 hoist 失效之 env-repair adjacent issue 已分兩階段結案：
> - **048 期間 retrospective**（2026-05-25）：commits `d521c819`（pnpm-workspace.yaml 加 nodeLinker: hoisted、修 host build）+ `b4453385`（Dockerfile pnpm 10.18.0 → 11.0.8 + --ignore-scripts、host + container 對齊）、user 拍板 cross-boundary minimal；retrospective 認定為 TS-DepGraph-Hygiene 軌道 047.5 retro-member。
> - **049 base-web-dep-hygiene**（2026-05-25）：TS-DepGraph-Hygiene 軌道首發 sprint、結案 (a) hoist 策略 review（strict isolation + explicit devDeps 取代 hoisted）+ (b) explicit devDep declarations 完整 audit + (c) packageManager pin 進 package.json（取代 Dockerfile ARG + host system 雙來源）。
> - **未結案 (d)**：「下次 pnpm 升級重新檢視」屬 trigger-driven、留 backlog 等 pnpm 12 或 newer corepack 強制 sha hash 等觸發訊號出現。

### 規劃中、未排程（design 規劃、待排 feature）

| ID | 出處 | 項目 | 備註 |
|---|---|---|---|
| F1.2 | F1 拆分（F1.1 已交） | JWT algorithm 升級 + key versioning + refresh-token 預埋 | 原規劃 with F10；refresh token 已由 F10 / F13 另路完成，F1.2 剩餘範圍（algorithm / key versioning）待重新界定 |
| W-F6b | W-F6 留下 | acme.sh 真實 cert acquisition / renew 流程 | 需公網 + 真實 domain + DNS provider creds |
| W-F15/16 | DESIGN-W-DEPLOYMENT §11 | backup-job + DR 流程（含 `backup_completed_total` metric 實 instrument，044 留 declared 0 series + dashboard panel placeholder） | Phase W deploy 後續、未排程 |

---

## ✅ 已完成里程碑

- [x] outer git init + push（`miso168net/fork260509-rev1`、default branch `rev1-admin-root`）
- [x] worktree + submodule 配置（`base-web` / `rust-api` 雙重身分；CLAUDE.md §4 操作手冊）
- [x] spec-kit v0.8.7 + extensions（before_specify pre-hook → `speckit.git.feature`）
- [x] constitution v1.0.0（`.specify/memory/constitution.md`、Principle I-V）
- [x] **F4 response-shape-alignment** ✅（2026-05-12 完成；outer `3d357e5`、rust-api `82bbde5`；spec `specs/001-response-shape-alignment/`）— rust↔base-web API 回應 envelope 形狀對齊
- [x] **F3 soft-delete-infrastructure** ✅（2026-05-14 完成；outer `6941788` + merge `0f1c5c3`、rust-api `2a65e2c`；spec `specs/002-soft-delete-infrastructure/`）— 7 張 admin 表加 deleted_at 軟刪基礎設施
- [x] **F2.1 audit-log-infrastructure** ✅（2026-05-14 完成；outer `3c9c689` + merge `209a2c8`、rust-api `6bfa674`；spec `specs/003-audit-log-infrastructure/`）— audit log 基礎設施（F2.2 outbox + Redis TTL 留後續）
- [x] **F1.1 jwt-secrets** ✅（2026-05-15 完成；outer `c582db4` + merge `5f82df3`、rust-api `65ce06a`；spec `specs/004-jwt-secrets/`）— JWT secret 外部化（F1.2 algorithm 升級 / key versioning 留後續）
- [x] **F5.1 auth-login-and-dynamic-menu** ✅（2026-05-15 完成；outer `be6e237` + merge `e71aefe`、rust-api `a85e88c`；2026-05-18 follow-up patch outer `1bdbc2f` + rust-api `1219418`；spec `specs/005-auth-login-and-dynamic-menu/`）— base-web 登入 + 動態 menu，P2 主體解鎖
- [x] **W-F1 dockerfile-rust-api** ✅（2026-05-15 完成；outer `bdbfb3c` + merge `430ada9`、rust-api `6831677`；spec `specs/006-dockerfile-rust-api/`）— rust-api Dockerfile + /health endpoint，Phase W deploy P1
- [x] **W-F2 dockerfile-base-web** ✅（2026-05-15 完成；outer `de1df10` + merge `ac79ed0`、base-web `cb897e9`；spec `specs/007-dockerfile-base-web/`）— base-web Dockerfile（node builder + nginx runtime）
- [x] **W-F3 compose-base-structure** ✅（2026-05-15 完成；outer `aa23840` + merge `04671d0`、base-web W-F2 followup `d56b9f8`；spec `specs/008-compose-base-structure/`）— outer docker-compose 基礎結構（5 service stack）
- [x] **W-F4 secret-injection** ✅（2026-05-15 完成；outer `b3d027e` + merge `ab658d7`、rust-api `4057770` + `adf5f4c`；spec `specs/009-secret-injection/`）— Docker secrets `_FILE` pattern，Phase W deploy P1 收尾
- [x] **W-F5 front-nginx** ✅（2026-05-16 完成；outer `101c9ac` + merge `dff14c2`；spec `specs/010-front-nginx/`）— front-nginx 反向代理 + SPA gateway，Phase W deploy P2
- [x] **W-F7 port-mapping** ✅（2026-05-17 完成；outer `ad239c4` + merge `62b3475`；spec `specs/011-port-mapping/`）— dev host port mapping（docker-compose.dev.yml）
- [x] **W-F6 tls-cert-management** ✅（2026-05-18 完成；outer `38cd074` + merge `5e38030`；spec `specs/012-tls-cert-management/`）— TLS 終止 + prod cert lifecycle skeleton
- [x] **F6 route-guard** ✅（2026-05-18 完成；outer `b079248` + merge `a431215`、rust-api `1649aa9`；spec `specs/013-route-guard/`）— rust-api `/route/isRouteExist` endpoint 補完 vue-router guard
- [x] **W-FA1 compose-nestjs-service** ✅（2026-05-18 完成；outer `b2c3ff7` + merge `b095d55`；spec `specs/014-compose-nestjs-service/`）— nestjs service 加進 compose（profile track-a）
- [x] **W-FA3 cicd-nestjs-build-job** ✅（2026-05-18 完成；outer `68c5c4e` + merge `f23f38e`；spec `specs/016-cicd-nestjs-build-job/`）— nestjs image build script，Track DESIGN-A 三件套收尾
- [x] **W-FA2 nginx-track-a-transitional-block** ✅（2026-05-18 完成；outer `e8f4dac` + merge `c5b7840`；spec `specs/015-nginx-track-a-transitional-block/`）— nginx refreshToken→nestjs TRANSITIONAL block
- [x] **F11 extracted-stubs** ✅（2026-05-19 完成；outer `6299640` + merge `81ecb0d`、rust-api `c58e619`；spec `specs/020-extracted-stubs/`）— DESIGN-A 抽離項清單 4 條 stub endpoint
- [x] **F10.2 rust-tokenstatus-string-align** ✅（2026-05-19 完成；outer `d53e724` + merge `851ec79`、rust-api `cb97224`；spec `specs/019-rust-tokenstatus-string-align/`）— rust TokenStatus `unused`/`used` 字串對齊
- [x] **F10.1 rust-jwt-refresh-token-signing** ✅（2026-05-19 完成；outer `bd11d2b` + merge `48b70e6`、rust-api `211ef74`；spec `specs/018-rust-jwt-refresh-token-signing/`）— rust 簽 HS256 JWT refresh token
- [x] **F10 refresh-token-nestjs-bridge** ✅（2026-05-19 完成；outer `4d6fc28` + merge `8f0e84c`；spec `specs/017-refresh-token-nestjs-bridge/`）— refreshToken→nestjs bridge wire-up，application Phase 4 起點
- [x] **F7.1 fix-route-getuserroutes-wiring** ✅（2026-05-20 完成；outer `554436f` + merge `efe910e`、rust-api `4de171a`；spec `specs/023-fix-route-getuserroutes-wiring/`）— 修 `/route/getUserRoutes` 500 wiring + menu paginated wrapper
- [x] **F7.2 role-code-alignment** ✅（2026-05-20 完成；outer `68410df` + merge `476ca88`、rust-api `84cbc19`；spec `specs/024-role-code-alignment/`）— getUserInfo role code alias 映射（ROLE_*→R_*）
- [x] **F8 assign-users** ✅（2026-05-20 完成；outer `f1bfff7` + merge `c2b0912`、rust-api `90f37d1`；spec `specs/025-assign-users/`）— assign-users user-role 指派 endpoint，application Phase 3 收尾
- [x] **F7 manage-crud-alignment** ✅（2026-05-20 完成；outer `44c7424` + merge `136b1eb`、rust-api `11b888c`；spec `specs/022-manage-crud-alignment/`）— manage/* 4 模組 CRUD 讀側形狀對齊
- [x] **F9 systemManage-alias-router** ✅（2026-05-20 完成；outer `260f2c6` + merge `b2f910c`、rust-api `b744c8e`；spec `specs/021-systemmanage-alias-router/`）— `/systemManage/*` alias router（10 條 alias endpoint）
- [x] **W-F11 rust-horizontal-scaling** ✅（2026-05-21 完成；outer `d02c7c9` + merge `d2d4c4c`、rust-api `d122b23`；spec `specs/026-rust-horizontal-scaling/`）— rust-api 水平擴展（Casbin redis pub-sub + 多 replica），Phase W-4
- [x] **F12 cleanup-job** ✅（2026-05-21 完成；outer `78e585c` + merge `86e56e5`、rust-api `30c8dd4`；spec `specs/027-cleanup-job/`）— cleanup binary 物理清除過期軟刪 row，DESIGN-A 本體 F1–F12 收尾
- [x] **F14 design-a-to-b-cutover** ✅（2026-05-21 完成；outer `33758f0` + merge `1f20a0d`、rust-api `729d3c6`；spec `specs/029-design-a-to-b-cutover/`）— DESIGN-A→B cutover、nestjs 完全退場，DESIGN-B 形態生效
- [x] **030 systemmanage-status-gender-alignment** ✅（2026-05-21 完成；outer `681dcbe` + merge `0ed2e85`、rust-api `0e1fb95`；spec `specs/030-systemmanage-status-gender-alignment/`）— systemManage status/gender enum 契約對齊
- [x] **F13 rust-refresh-token-impl** ✅（2026-05-21 完成；outer `a1e739c` + merge `4e9cf07`、rust-api `d019d8f`；spec `specs/028-rust-refresh-token-impl/`）— rust 自實作 refresh token 輪替，DESIGN-A §6.1 P5 起點
- [x] **034 role-authorization-wiring** ✅（2026-05-22 完成；outer `bead3fd` + merge `ff5ea63`、rust-api `e9787ed`、base-web `7325b091`；spec `specs/034-role-authorization-wiring/`）— base-web 角色菜單授權接線（menu-auth-modal 讀/寫接 rust `/systemManage/{getRoleMenuIds,assignRoleMenus}` 2 alias）+ assign_routes 空清單 clear-all 修正，13/13 C-V PASS，W-WEBUI 軌道第四個也是最後一個 feature
- [x] **033 role-crud-wiring** ✅（2026-05-22 完成；outer `fcf7280` + merge `729dbf9`、rust-api `536bf88`、base-web `ceafe62a`；spec `specs/033-role-crud-wiring/`）— base-web role CRUD 接線 + update_role status-drop 修正 + roleCode code-lock，W-WEBUI 軌道第三個 feature
- [x] **032 menu-crud-wiring** ✅（2026-05-22 完成；outer `d96aafa` + merge `8ccc4b4`、rust-api `149dc52`、base-web `b43634c0`；spec `specs/032-menu-crud-wiring/`）— base-web menu CRUD 接線，W-WEBUI 軌道第二個 feature
- [x] **031 user-crud-wiring** ✅（2026-05-22 完成；outer `10edf43` + merge `a09d316`、rust-api `2a24e9d`、base-web `1793b361`；spec `specs/031-user-crud-wiring/`）— base-web user CRUD 接線，W-WEBUI 軌道第一個 feature
- [x] **035 user-role-and-password-wiring** ✅（2026-05-23 完成；outer `4332a49` + merge `7020899`、rust-api `1de553e`、base-web `003de689`；spec `specs/035-user-role-and-password-wiring/`）— W-FW5；補完 W-FW1 留下的 user→roles 指派（rust `SystemManageUserOutput.user_roles` 真實批次填充 + transform DTO 收 `userRoles` delta 寫 `sys_user_role` 含 audit）+ 密碼 UX（修 `update_user` 密碼未 hash pre-existing bug + user 抽屜選填 password 欄 + 新增 `POST /auth/changePassword` 自助改密碼端點 + user-center 修改密碼面板），20/20 C-V PASS，W-WEBUI follow-up 軌道第一個 feature；含 `change_password` audit actor 型別 build-gate 修正 + user 抽屜角色欄 mock scaffolding 殘留去重（W-FW1 遺留、被 user_roles 真實填充曝出）2 個收尾修正
- [x] **036 menu-field-persistence** ✅（2026-05-23 完成；outer `1a77cff` + merge `dd7de46`、rust-api `8116080`、base-web 0 改動；spec `specs/036-menu-field-persistence/`）— W-FW7；補完 W-FW2 留下的 menu 欄位持久化缺口（W-FW2-N1）—— `sys_menu` 加 `query`/`buttons` (JSONB nullable) + `fixed_index_in_tab` (INTEGER nullable) 三欄、entity 同步、寫入路徑透傳（systemManage transform → native MenuInput → menu service）、admin CRUD 讀回（`SystemManageMenuOutput.From<MenuTree>` 填真值）、runtime 動態路由 `RouteMeta` 加 `query`+`fixedIndexInTab`（buttons 不進、Q2 拍板），20/20 C-V PASS（含 CDP modal 預填驗證），W-WEBUI follow-up 軌道第二個 feature；base-web 0 改動（exploration + Phase 0 R-Q1/R-Q2 驗證 modal UI 與 null 防護齊備）；Constitution v1.2.0「W-WEBUI 受管例外」第二次行使；既有 audit 路徑（`audit_log::write_in_txn` + `audit_snapshot`）自動涵蓋新 3 欄無需新邏輯
- [x] **037 role-authorization-completion** ✅（2026-05-23 完成；outer `90128e1` + merge `10b5bee`、rust-api `81c31d11`、base-web `bfa1494d`；spec `specs/037-role-authorization-completion/`）— W-FW6；整併 W-FW3/W-FW4 過渡留下的 3 個 follow-up：**N2 role home 持久化**（`sys_role` 加 `home_route_name VARCHAR NULL` + Casbin policy seed 4 row + 2 systemManage 端點 `getRoleHome/:roleId` GET + `updateRoleHome` POST + validation reject 不存在 / soft-deleted / constant menu + base-web menu-auth-modal `getHome`/`updateHome` 接真 API + 'home' sentinel → NULL）；**N3 `assign_routes`/`assign_users` audit gap**（trait signature 末尾 append `actor: &Actor` + `audit_log::write_in_txn` 於 txn commit 前 + whole-snapshot before/after `menuIds`/`userIds` camelCase + cascade 3 callsite + `assign_permission` 不動推遲 W-FW8）；**N4 role code 安全改名**（`update_role` detect `before.code != updated_role.code` 時 txn 內 `UPDATE casbin_rule SET v0=$1 WHERE ptype='p' AND v0=$2` + commit 後 `notify_casbin_changed()` 經 W-F11 redis pub-sub 觸發 enforcer reload + 拿掉 W-FW3 transform-layer code-lock）；C-V1~C-V27 全 27 條 PASS（curl + psql + Casbin reload log + CDP browser smoke via Edge :9229；C-V23 menu-auth-modal home dropdown E-1 fallback + pick + reopen pre-fill + clear；C-V24 role 編輯抽屜 roleCode editable + rename + 列表反映新 code + audit trail）；W-WEBUI follow-up 軌道第三個 feature；Constitution v1.2.0「W-WEBUI 受管例外」第三次行使（base-web 改 2 檔：system-manage.ts + menu-auth-modal.vue）；rust-api 改 14 處 + 2 新 migration；含 Code Quality Review feedback fix（assign_routes/assign_users audit payload JSON keys 改 camelCase 對齊 rev1 慣例）
- [x] **038 button-auth-completion** ✅（2026-05-23 完成；outer `fed76b5` + merge `5c9ea69`、rust-api `610e136`、base-web `08bbe315`；spec `specs/038-button-auth-completion/`）— W-FW8；W-WEBUI follow-up 軌道收尾 feature；整併 W-FW4/W-FW6 過渡留下的最後 2 follow-up：**US1 button-auth modal 接通**（3 systemManage alias 端點 `getAllEndpoints` GET + `getRoleEndpointIds/:roleId` GET + `assignRoleEndpoints` POST + Casbin policy seed 6 row + base-web `button-auth-modal.vue` 拿掉 10 mock + 接通 3 endpoint API + Promise.all 並行 fetch + setLoading + toast + Casbin server-side enforce 真實生效）；**US2 assign_permission audit 補寫**（trait + impl signature 末尾 append `actor: &Actor` + `audit_log::write_in_txn`（payload camelCase `{roleId, domain, endpointIds:[...]}`, entity_type='sys_role', operation=Update, source=Internal）+ 空陣列允許清空 reconciliation（reject 條件改 `if !raw_input.is_empty() && valid_permissions.is_empty()` 同 assign_routes 體例）+ cascade 2 callsite handler；落地後 grep `audit_log::write_in_txn` in sys_authorization_service.rs 命中 3 次，W-FW6 N3 留下的最後 audit gap 補齊）；C-V1~C-V27 全 27 條 PASS（curl + psql + Casbin reload log + CDP browser smoke via Edge :9229；C-V19 ROLE_SUPER 編輯抽屜 + 按钮权限 modal 開 + tree 12 group render；C-V20 R_WFW8_TEST modal submit + 「修改成功」toast；audit C-V13/14/15/16 + 軟刪 edge case C-V26/27）；W-WEBUI follow-up 軌道第四個也是最後一個 feature（**軌道全部清空**）；Constitution v1.3.0「W-WEBUI 受管例外擴 W-FW1–W-FW8」首次行使（base-web 改 2 檔：system-manage.ts + button-auth-modal.vue）；rust-api 改 12 處 + 1 新 data migration；0 schema migration、0 新 table、0 nestjs；下一步：observability（W-F12/13/14）
- [x] **050 spec-hygiene-pass-4** ✅（2026-05-25 完成；outer `3243fe6` + merge `dd872ce`、base-web `64af823b`、rust-api `1a7ef2a`；spec `specs/050-spec-hygiene-pass-4/`）— post-merge code review 衍生 5 Important + 1 Polish bundled mixed sprint：036-R1 sys_menu selective merge (`Option<Option<T>>` double-option DTO + `update_menu_selective` service handler) + 037-R1 Casbin `ptype='g'` defensive UPDATE + `g_rules_updated_count` audit (per Clarifications Q2 MUST) + GeneralUser deny C-V + 044-R1 install prometheus pushgateway service (`prom/pushgateway:v1.10.0` per Clarifications Q1) + cleanup binary recorder `install_pushgateway_recorder()` + scrape job (`honor_labels: true`) + rustls CryptoProvider follow-up fix + 044-R2 axum `MatchedPath` route template (cardinality 限制：3 numeric ID curls → 單一 `/systemManage/getRoleHome/{roleId}` series) + 046-R1 FR-015 generalized expansion budget amend wording + retro footnote + 049-R1 Dockerfile strict isolation 紀律 comment polish；軌道內 TS-DepGraph-Hygiene 1 條 (049-R1、DESIGN-W-BASE-WEB §4.4.3 dynamic 行使首次) + 軌道外 5 條 (rust-api 4 file + cleanup recorder + spec docs + infra docker-compose pushgateway + dev port 19091)；0 Constitution amendment、0 base-web src/ diff、0 schema migration、0 new entity、0 new endpoint；C-V1~C-V10 全 PASS（C-V2 partial update preserve / C-V3 g sync + audit + deny / C-V4 cleanup_job series end-to-end via pushgateway → prometheus / C-V5 route template / C-V6 spec amend / C-V7 Dockerfile / C-V8 governance docs / C-V9 boundary）；連帶 R-row 038-R1 + 039-R1 Critical 留 dedicated sprint backlog
- [x] **049 base-web-dep-hygiene-and-track-restructure** ✅（2026-05-25 完成；outer `b860cc6` + merge `cd5114f`、base-web `f6efe906`、rust-api 0 改動；spec `specs/049-base-web-dep-hygiene/`）— TS-DepGraph-Hygiene 軌道首發 sprint（Constitution v1.6.0 同步落地、軌道 governance 從 3 doc 收成 1 unified doc DESIGN-W-BASE-WEB.md）：comprehensive audit + 提升 4 phantom transitive 為直接 devDeps（@iconify/utils@3.1.0 + axios@1.13.6 到 base-web、@unocss/core@66.6.6 + @unocss/preset-mini@66.6.6 到 packages/uno-preset）+ 移除 nodeLinker: hoisted 回 pnpm 預設 strict isolation + 移除 .npmrc shamefully-hoist=true vestige + 加 packageManager: "pnpm@11.0.8" pin 進 package.json + Dockerfile remove ARG PNPM_VERSION/corepack prepare（改 corepack enable + 註解 outdated cleanup）+ 順道修 unocss.ts:24 implicit any；軌道內 base-web build/dep config 7 file + 軌道外 outer（Constitution amendment v1.5→v1.6 + DESIGN-W-WEBUI.md → DESIGN-W-BASE-WEB.md rename + restructure + TS-Typing-Sync content merge + DESIGN-W-TYPING-ALIGN.md 刪除 + plan-template 4-軌道辨識 + CLAUDE §1/§7 索引）；0 rust-api 改動、0 base-web src/ diff、0 schema migration、0 新 cargo dep、0 新 npm dep（純 transitive → direct 提升）；Constitution 5/5 PASS（post-v1.6.0 amendment）；C-V1~C-V10 全 PASS（pnpm install --frozen-lockfile + vue-tsc + vite build + docker rebuild healthy + CDP smoke 8/8 reuse 048 cdp-smoke.js + grep audit + boundary verify + DESIGN docs + INTEGRATION-CHECKLIST cleanup）；048-N1 follow-up (a)+(b)+(c) 結案、(d) trigger-driven 留
- [x] **048 base-typings-sync** ✅（2026-05-25 完成；outer `b903ead` + merge `f2e2177`、base-web `4e05d478`、rust-api 0 改動；spec `specs/048-base-typings-sync/`）— TS-Typing-Sync 軌道首發 sprint（Constitution v1.5.0 同步落地）：3 mismatch fix（MenuRoute.id string→number + 加 pid string、MenuTree.pId→pid rename + retype）+ 3 JSDoc audit（CommonRecord.id Snowflake 53-bit / Menu.parentId root sentinel / Auth.UserInfo.userId ULID vs display_id）；軌道內 base-web 4 檔 typings/api edit、軌道外 outer（amendment + 新 DESIGN-W-TYPING-ALIGN.md + plan-template 軌道辨識條目 + CLAUDE §1/§7 索引）；0 rust-api 改動、0 schema migration、0 新 cargo dep；env-repair adjacent commit `d521c819` 修 pnpm 11 hoist 失效（pnpm-workspace.yaml 加 nodeLinker: hoisted、user 拍板 cross-boundary minimally、登 048-N1 long-term review）；Constitution 5/5 PASS（post-v1.5.0 amendment）、含 amendment v1.4.0→v1.5.0；C-V1~C-V9 全 PASS（C-V6 CDP smoke 8/8 PASS、cdp-smoke.js 為 implementer stage 自寫）
- [x] **047 sandbox-protect-route-fix** ✅（2026-05-25 完成；outer `003105d` + merge `dd45dc1`、rust-api `cca44c5`、base-web 0 改動；spec `specs/047-sandbox-protect-route-fix/`）— 045-N1 結案：api_key_middleware 兩 error 分支 HTTP 200→401 + WWW-Authenticate: ApiKey header + body envelope 保留（覆蓋 Simple+Complex validator 雙 path、雙 sandbox endpoint 同步生效）+ 8 unit test（Simple 4 + Complex 4、Q1 拍板雙覆蓋、tower::ServiceExt::oneshot + unique path 紀律）；軌道外 rust-api + outer、0 base-web、0 schema migration、0 新 entity、0 新 workspace dep（server-core dev-deps 加 tower util + tokio macros/rt-multi-thread 為 per-crate change）；Constitution 5/5 PASS、0 amendment
- [x] **046 spec-hygiene-pass-3** ✅（2026-05-25 完成；outer `6ed9d6c` + merge `f188dd0`、rust-api `807d7bb`、base-web 0 改動；spec `specs/046-spec-hygiene-pass-3/`）— bundle 045-N2 + 044-N1 + 042-N1 + docker-compose footer trivial 4 follow-up = US1 045 `data-model.md §E1.2` 兩處 erratum（whole-Model `==` 改 6 業務欄位 `same_business` semantic compare 描述 + UPDATE 路徑 preserve `before_row.created_at` + `display_id` + tracing macro `target: target` 改 `target = target` structured field）+ US2 045 `contracts/verification-commands.md` 5 處對齊實 code（C-V2 schema column `entity_type` → `module_name`、operation case `Insert/Update` → `INSERT/UPDATE`、C-V4-V6 access_key endpoint `/api/accessKey` → `/api/access-key` + create body 加 `"domain":"dev"`、C-V8/V9 addUser body `username` → `userName` + `status:"enabled"` → `status:"1"` + `userRoles:[i64]` → `userRoles:["ROLE_..."]` 形、C-V10 updateUser HTTP verb `PUT` → `POST` + URL 拿掉 path param + body 加 `"id"`）+ US3 `rust-api/server/service/src/admin/sys_authorization_service.rs` 3 處 production `println!`（lines 140/154/168）改 `tracing::debug!(?var, "assign_permission: <name>")` structured field（W-F12 fmt::json formatter 對齊、Loki ingest 全 valid JSON line）+ US4 新 `rust-api/server/model/tests/common/audit_pipeline.rs` 暴 `wait_for_audit_row<F, Fut>(closure, timeout_ms)` + `wait_for_audit_count<F, Fut>(closure, min_count, timeout_ms)`（500ms budget / 50ms interval / closure-based 對齊 Sea-ORM Entity::find().one()）+ `common/mod.rs` 加 `pub mod audit_pipeline` + 4 test file（audit_basics 5 + audit_http_middleware 2 + audit_transaction_rollback 2 + soft_delete_audit_integration 3）共 12 個 `#[ignore]` 註解更新為 `"requires dev stack drainer running (audit outbox → sys_operation_log async pipeline)"` + 6 個 polling-needs callsite 改用 helper（其餘 6 個為 audit_snapshot 純單測 + rollback/not-found absence 測試、保留原 query pattern 不動）+ US5 `docker-compose.yml` 檔頭註解擴寫「主 stack 8 service + docker-compose.observability.yml 7 service = dev 12 service」+ implementer-stage scope expansion（user 拍板加大 ≤3 budget）= 5 test file 17 處 `display_id: i64` (sea_orm ActiveModel: `Set(Local::now().timestamp_nanos_opt().unwrap_or(1))` / Model: `0i64`) 與 1 處 `sys_role::Model.home_route_name: None` 補上（039 display_id migration + 037 home_route_name 漏 propagate 到測試碼、預先存在 E0063 編譯阻擋）+ server-model dev-dep `tokio` 加 `"time"` feature（helper `tokio::time::sleep` 必要、per-crate dev-dep change、非 workspace 新 dep）；軌道外 rust-api + outer + spec md、0 base-web、0 schema migration、0 新 entity、0 新 workspace cargo dep；cargo check --tests -p server-model PASS；下一步：047 sandbox-protect-route-fix (045-N1) 或 base-web TS id 型別債 cleanup sprint
- [x] **045 facade-atomicity-pass** ✅（2026-05-25 完成；outer `6681cf6` + merge `7daa086`、rust-api `e62c7aa`、base-web 0 改動；spec `specs/045-facade-atomicity-pass/`）— bundle F3-N1 + F3-N2 + F3-N3 + 035-N1 四 follow-up = US1 sys_endpoint facade `upsert_with_audit` + service `sync_endpoints` 改走 facade（business-field semantic compare 取代 whole-Model `==`、UPDATE 保留 `created_at` + `display_id`、outer txn 全 endpoint upsert 共享 single all-or-nothing scope）+ US3 facade `batch_soft_delete_with_audit` + `BatchDeletePolicy::{FailFast, LogAndContinue{target}}` enum + `BatchDeleteResult{ok, failed}` + service `batch_remove_endpoints` 改走 facade（per-id own-txn + log-and-continue `target="endpoint_sync"`）+ US2 sys_access_key DB↔in-memory atomicity（redis pub-sub channel `api_key:invalidate` + `notify_api_key_changed` publisher + `spawn_api_key_sync_subscriber` 5s backoff loop + 044 `.instrument(Span::current())` trace propagation + W-F11 體例對齊 + 2 新 metric `api_key_invalidate_total` / `api_key_reload_total` 044 pre-declare 模式 + `delete_access_key` 拿掉 inline `remove_key` 改 1 行 `notify_api_key_changed`）+ US4 TUserService 3 `*_in_txn` 雙生方法（`create_user_in_txn` / `assign_roles_to_user_in_txn` / `update_user_in_txn`、簽名 `&DatabaseTransaction`、原 fn 改 thin delegate + 030-040 callsite 0 改動）+ systemManage `add_user_for_systemmanage` / `update_user_for_systemmanage` handler 改開外層 txn（C-V8 atomic commit span 4.4ms / C-V9 invalid role rollback user+INTERNAL audit 0 + HTTP middleware audit 1 per 042 outcome-agnostic / C-V10 update happy+negative rollback 同模式）；軌道外 rust-api + outer、0 base-web、0 schema migration、0 新 entity；C-V1~C-V12 acceptance 11 PASS + 1 blocked-by-pre-existing-sandbox-bug（C-V5 sandbox endpoint 對任何非空 x-api-key 都 200、屬 045-N1 預先存在的 protect_route 失效；045 機制本身經 C-V4/V6 metric + subscriber log + DB `deleted_at` 三方證實正常）；spec docs spec rot 多處（data-model.md §E1.2 + contracts/verification-commands.md C-V2/V4/V8/V10）登記 045-N2 留 spec-hygiene-pass；下一步：base-web TS id 型別債 cleanup sprint 或其他 follow-up backlog
- [x] **044 observability-and-cleanup-pass** ✅（2026-05-24 完成；outer `95b8f41` + merge `c03d7a3`、rust-api `2ed707c`、base-web 0 改動；spec `specs/044-observability-and-cleanup-pass/`）— W-F12/13/14 三件套 + P2 4 項 cleanup bundled = 7 observability service (promtail+Loki+prometheus+grafana+3 exporters) + rust tracing JSON migration (fmt::json + tower-http TraceLayer span-based `http_req` span attrs `service`/`request_id`/`method`/`route` + 042 task-local 並存) + 8 業務 metric (6 active instrument: `audit_log_writes_total` 雙寫 + `casbin_enforcement_total` + `casbin_policy_cache_invalidate_total` + `outbox_pending_events` + `cleanup_job_rows_deleted_total` + `http_request_duration_seconds` / 2 declared 0 series: `sys_tokens_active` + `backup_completed_total`) + 7 spawn callsite `.instrument(Span::current())` propagation + grafana 6 dashboards (1 master + 5 drill-down per Clarifications Q2) + 6 alert rules (grafana built-in unified alerting per Q1: RustApi5xxRate + AuditPipelineStalled + OutboxQueueBacklog + PostgresConnectionSaturation + RedisMemoryHigh + LogVolumeDrop) + dev default on 12 service (per Clarifications Q3); P2 4 cleanup: US4 `extract_entity_id_from_url` systemManage hybrid rule (model/admin/audit_log.rs:196)、US5 `OperationLogContext.module_name`/`description` 實值推導 + url_to_entity_type 移到 server-core (修反向 dep) (core/web/operation_log.rs:121-141)、US6 `print!("user is")` 1 處清理 (sys_user_api.rs:40 → tracing::debug!)、US7 status enum errata 結案 (grep 0 hit、留 045 候選); 軌道外 rust-api + outer、0 base-web、0 schema migration、0 新 entity、0 nestjs；Phase W deploy P5 close-out (W-F12/13/14 全結案)；implementer 階段發現新 follow-up 044-N1 (`sys_authorization_service.rs` 3 處 production `println!`、research.md R-3 漏抓、登記 backlog 待後續清); 下一步：P3 F-facade-atomicity-pass 或 base-web TS id 型別債 cleanup sprint
- [x] **043 spec-hygiene-pass-2** ✅（2026-05-24 完成；outer `bbb1b04` + merge `b4b21df`、rust-api 0 改動、base-web 0 改動；spec `specs/043-spec-hygiene-pass-2/`）— 042-N3 + R5 + 041-N2 bundled = 12 處 spec md edits：10 處 TZ fix（spec 003/021/042、`(NOW() AT TIME ZONE 'UTC')::timestamp - INTERVAL` pattern、修 postgres NOW() timezone-aware vs sys_operation_log.created_at UTC naive 的 8h offset 誤差）+ spec 005 contracts/auth-endpoints.md 新 §7 Logout (no server endpoint by design)（含 token revocation 3 pattern 對照 Redis blacklist / Short-TTL refresh / JWT versioning + W-F12 observability hook）+ spec 002 §E4 line 158 use 行對齊（use server_model::admin::entities → use crate::admin::entities）；軌道外 pure spec md、0 rust-api / 0 base-web、0 schema migration、0 新 cargo crate dep；C-V1~C-V5 全 PASS（C-V2 dev stack 真實 acceptance：改前 0 row / 改後 2 row 雙視角）；implementer 階段擴展 1 處：本 feature contracts/verification-commands.md C-V3 grep pattern `\|` 在 ERE 下被 escape literal → 改 `|` alternation（自家 contract polish、非 scope creep）；INTEGRATION-CHECKLIST 移除 042-N3 / R5 / 041-N2 三 row、下一步指向 W-F12/13/14
- [x] **042 audit-outbox-and-http-mount** ✅（2026-05-24 完成；outer `c3aadd3` + merge `36d7919`、rust-api `7b13554`、base-web 0 改動；spec `specs/042-audit-outbox-and-http-mount/`）— R3 + F2.2 + R2 bundled = 新 `sys_audit_outbox` 表為 audit event 耐久暫存區 + background drainer 推 Redis Stream `audit:events` + sys_operation_log；Pattern A unified outbox-first；OperationLogLayer 統一 mount 在 apply_layers innermost（task-local request_id 傳到 INTERNAL audit 同步串聯）；HTTP middleware audit 補完 spec 003 C8 期望（每 write 2 row：INTERNAL service-level + HTTP middleware）+ R2 失敗登入 audit 自動結案；軌道外 rust-only + spec md；C-V1~C-V11 全 PASS、C-V12 Part A latency 0.96ms ≪ 50ms threshold (SC-004) / Part B p50=175ms 落在 spec 預測 100-200ms 範圍（drainer sleep_interval=50ms）；implementer 階段發現修正：URL prefix `/api/` 在 rust-api 內被 front-nginx `proxy_pass http://rust_api/` strip 為 `/`（spec data-model.md §E2 + verification-commands.md 對應更新）+ POST `/api/role` payload 需含 `pid` 欄（W-FW6 後 sys_role.pid NOT NULL）+ multi-replica scale=2 因 dev compose 11081 port 單實例 bind 受限（測試以單實例 50 並發代理、SKIP LOCKED + per-row idempotency 行為驗證）；引入 callback registry pattern（`HttpAuditWriter` in server-global、避 server-core→server-model reverse dep）+ `AuditEventOwned` twin（borrowed serialize / owned deserialize）+ `tokio::task_local!` request_id 透過 OperationLogLayer.scope(...) 傳到 service-level write_in_txn（030-040 callsite 0 改動、HTTP↔INTERNAL row 同 request_id 串聯）；application.yaml drainer_sleep_interval_ms=50 / batch_size=100 / max_retry=5 / redis_stream_maxlen_approx=10000；下一步：W-F12/13/14 observability
- [x] **041 spec-hygiene-pass-1** ✅（2026-05-24 完成；outer `1244cd3` + merge `2c40766`、rust-api `8ccc08d`、base-web 0 改動;spec `specs/041-spec-hygiene-pass-1/`）— R4 (6 處 spec rot) + F3-N5 (F3 階段遺留) bundled = 7 處 spec md errata + rust-api 全局 NormalizePathLayer wrap（FR-008/009、跨 admin/auth/authorization 一致）;軌道外 rust-only + spec md;implementer 階段發現 3 處擴展修正：030 C-V9 `id` post-039 為 i64 應取 `display_id`（030 errata 擴展第 7 項）/ 041 spec.md C-V5 sample `/api/menu/` 實 mount 在 `/route/`（spec 自身修正）/ 039 C-V31 acceptance 用 Administrator/有 role user 而非 CV041Test（無 role 被 casbin 拒）;rust-api 改 2 檔（server/bin/Cargo.toml + server/bin/src/main.rs、原 plan 寫 server/initialize/Cargo.toml + main.rs、server/bin 才需 tower 直 dep 是 plan 漏抓）;新衍生 follow-up 2 條登記（041-N1 status enum 混雜 / 041-N2 §E4 use 行）
- [x] **040 wire-id-consistency** ✅（2026-05-24 完成；outer `7686ec1` + merge `34dc03d`、rust-api `21374ea`、base-web `496f301b`；spec `specs/040-wire-id-consistency/`）— W-FW9；039 落地後 wire-side consumer cleanup 4 themes bundled：**A** 修 critical 039 留下 modal body roleId String→number cascade（CDP smoke defer 漏看、curl 直送 number 過了 → 真實 runtime 撞 serde 422）—— `src/typings/api/system-manage.d.ts` 0 改動（R-Q5 grep 確認 Params 型不存在、全 service.ts inline type）、`src/service/api/system-manage.ts` 4 處 inline type `string → Api.SystemManage.Role['id']`、button-auth-modal.vue + menu-auth-modal.vue 各 2 處 `String(props.roleId)` 拿掉 + checks `shallowRef<string[]>` 對齊 NTree leaf key string shape + submit 時 `.map(Number)` boundary 轉 rust DTO i64；**B** URL path String() 餘料 4 處 A 副產品自然消；**C** 拆 032 留下 `deserialize_i32_or_string` workaround（~17 行）+ `SystemManageAddMenuInput/UpdateMenuInput.parent_id` `serde(deserialize_with)` 屬性、改 plain serde、base-web typings 已 number-only 安全；**D** 為 sys_role/sys_user/sys_access_key 3 entity 新增 `RoleDetail`/`UserDetail`/`AccessKeyDetail` wire DTO（`id: i64` 從 `model.display_id`、不含 password/secret）+ handler `.map(Detail::from)` wrap、sys_role_api.rs 5 + sys_user_api.rs 5 + sys_access_key_api.rs 2 共 12 handler 改 `Res<Detail>`、3 個 delete + 2 個 policy handler Res<()>/Res<bool> 不動、Sea-ORM Model 0 改動；sys_user service 返 `UserWithoutPassword` 額外加 `From<UserWithoutPassword> for UserDetail` impl（implementer 發現 cascade gap、1:1 field move）；C-V1~C-V28 多數 PASS（C-V1/V2 CDP smoke defer 同 039 慣例、C-V3/V4 base-web cleanup PASS、C-V6/V7/V8 parentId drop PASS、C-V9/V11/V12 wire DTO wrap PASS、C-V15-V22 039 regression PASS、C-V23/V24/V27 scope discipline PASS）；軌道內（A+B base-web 4 檔）+ 軌道外（C+D rust-only 8 檔）+ DESIGN-W-WEBUI §7 加 W-FW9 條目（Constitution v1.4.0 dynamic 首次行使）+ 0 amendment + 0 schema migration + 0 新 entity + 0 input DTO 改動（039 T030.5 已完成）；spec FR-001 含 errata note（plan R-Q5 + analyze F1 對齊）；三段式 commit（base-web 2 commit: 2989c2e + 496f301b concern-fix-on-top / rust-api 3 commit: d0112b1 + cf2cc03 + 21374ea / outer SHA pin + DESIGN-W-WEBUI 加 W-FW9）；下一步：observability（W-F12/13/14）
- [x] **039 rust-entity-id-numeric-migration** ✅（2026-05-24 完成；outer `34c605f` + merge `e53581b`、rust-api `7518926`、base-web 0 改動；spec `specs/039-rust-entity-id-numeric-migration/`）— 5 業務 entity（sys_user / sys_role / sys_endpoint / sys_organization / sys_access_key）加 `display_id BIGINT UNIQUE NOT NULL` 副欄 + Snowflake **41/5/7 = 53bit i64** generator（plan 階段發現原 spec 41/10/12 = 63bit 超 2^53 JS safe integer、user 拍板選 A: 41/5/7 變體填滿 2^53 - 1 = Number.MAX_SAFE_INTEGER；machine_id mod 32、self-roll 0 cargo dep）+ API 邊界 transform（output struct `id` 序列化為 `model.display_id` i64 / input DTO `role_id/user_ids/endpoint_ids/permissions` String→i64/Vec<i64> + handler entry 加 `lookup_ulid_by_display_id(i64)→ULID String` cascade / Path<i64> for raw entity 端點）；**rust internal SoT 100% 保留為 ULID**（audit_log payload + entity_id、JWT sub claim、Casbin policy、FK schema 0 改動，X1 雙欄設計核心）；C-V1~C-V28 23/23 全綠（C-V5 down 對稱與 C-V29/30 CDP smoke defer，C-V15 raw role 出 id ULID 兼 displayId i64 by design）；軌道**外** feature、**base-web 0 diff**、Constitution Principle IV「base 不改動邊界」**預設原則**回歸實踐（不動用受管例外、不觸發 amendment）；rust-api 改 ~38 處 + 2 新 migration（schema `m20260524_d` + backfill `m20260524_e`）+ 2 新 error file（sys_endpoint_error / sys_organization_error，code `CODE_BUSINESS_ENTITY_NOT_FOUND = 6001`）+ migration crate +1 dep（`server-global` path）；spec gap 補修：plan 階段發現 bit layout 數學矛盾（A-003）、執行 ⑤ 階段補 T030.5（8 個 body-level DTO `id/ids` 漏在 T019 範圍內、加 cascade 後 build clean + runtime acceptance）；T031 migration 首次跑撞 PG「cannot insert multiple commands into a prepared statement」（code reviewer 早 flag of important issue）→ chore fix `7518926` 拆 ALTER 2 條 `execute_unprepared`；下一步：observability（W-F12/13/14）或 base-web cleanup sprint（`String(roleId)` 餘料 + raw endpoint 仍 ULID id 統一）

---

## 📐 Roadmap & Phase 狀態

DESIGN-A §6.1 application features（F1–F14）與 Phase W deploy（W-F1~W-F7、W-F11、Track DESIGN-A W-FA1/2/3）**全部 ✅ 完成** —— 逐 feature 完成日 / commit SHA 見「已完成里程碑」，brainstorm / spec / plan / tasks 設計鏈見 `specs/<NNN>/`。

| Phase | 範圍 | 狀態 |
|---|---|---|
| Application P1 | F1–F4（jwt-secrets / audit-log / soft-delete / response-shape） | ✅ 2026-05-15 全完成 |
| Application P2–P3 | F5.1–F9（login+menu / route-guard / manage-crud / alias-router / assign-users） | ✅ |
| Application P4 | F10–F12（refresh-token 三件套 / extracted-stubs / cleanup-job） | ✅ DESIGN-A 本體 F1–F12 收尾 |
| Application P5 | F13–F14（rust-refresh-token-impl / design-a-to-b-cutover） | ✅ DESIGN-B 形態生效 |
| Phase W deploy P1–P2 | W-F1~W-F7（dockerfile / compose / secret / nginx / port / TLS） | ✅ |
| Phase W deploy P4 | W-F11（rust 水平擴展） | ✅ |
| Phase W deploy P5 | W-F12/13/14（observability：promtail+Loki + prometheus + grafana + 3 exporter + 8 業務 metric + 6 dashboard + 6 alert） | ✅ 2026-05-24 完成（044） |
| Phase W-7 Track DESIGN-A | W-FA1/2/3（nestjs compose / nginx transitional / cicd） | ✅ 完成 —— F14 後 nestjs 已退場（歷史） |

Phase 推進規則（P1 全完成才動 P2 等）見 design 文件 `DESIGN-A §6.1 / §6.2`、`DESIGN-W-DEPLOYMENT §11`。

**未排程**：W-F6b acme.sh 真實 cert acquisition / W-F15/16 backup-job + DR —— 見 Follow-up Backlog。

---

## 🔬 跨 feature 待驗證項

依 constitution Principle IV「上游驗證」—— feature spec.md Assumptions 段帶上、實作時驗、驗完勾掉並回填結果。

- [x] 預設密碼 `123456` / rev1 dev stack 起動 + migration 套用 —— 後續多個 feature acceptance 已實證（dev stack 自 W-F3 起常態運行）
- [ ] **F3 acceptance tests**（`cargo test --test soft_delete_basics -- --ignored`，3 檔共 9 個 `#[ignore]` test）—— 需 `TEST_DATABASE_URL` + migration up；迄今未正式跑
- [ ] **CI lint workflow 實際觸發**（`.github/workflows/ci-soft-delete-lint.yml`）—— rev1 走 worktree + merge 流程、無 PR，此驗證項須另定觸發方式或評估廢止

**F14 DESIGN-B cutover 前完整性盤點**（specs 001–028、2026-05-21）：28 feature 全 COMPLETE / COMPLETE-WITH-NOTES、0 GAPS；R1 / R3 / R4 已於 F14 處理（詳見 `specs/029-design-a-to-b-cutover/` 與 git history）。R2（F5.1 登入失敗無 audit）已於 042 audit-outbox-and-http-mount HTTP middleware audit pipeline 結案（見已完成里程碑 042 entry）。

---

## 📎 Brainstorming 決策快照

各 feature 的 brainstorm 拍板決策為 `docs/superpowers/<NNN>-feature.md`（spec-kit 設計鏈起點，與 `specs/<NNN>/` 對應）。本檔不重複 —— 查特定 feature 的決策請開該檔。

---

## 🛠 維護指引

feature 推進時，**在同一個 commit 內**更新本檔對應節：

| 時機 | 更新 |
|---|---|
| feature merge 回 default | 「已完成里程碑」加一條 entry（格式見下）；「Roadmap & Phase 狀態」對應 Phase 視需要更新 |
| brainstorm / implement / acceptance / review 發現衍生工作 | 「Follow-up Backlog」登記一條（標發現來源 + 階段） |
| follow-up 排程成 feature 並完成 | 從「Follow-up Backlog」移除（「已完成里程碑」會留紀錄） |
| 上游待驗證項驗完 | 「跨 feature 待驗證項」勾掉、結果回填 CLAUDE.md（操作事實）或 spec.md（feature-specific） |
| 每個 feature 收尾 | 「Current Focus」更新「現狀 + 下一步」 |

**里程碑 entry 格式規則**：「已完成里程碑」每條 feature entry 限**一行** —— 只記完成日期、outer / merge / worktree commit SHA、`specs/<NNN>/` 連結、一句話功能描述（體例見 F4 / F3 早期 entry）。**不抄**規格細節（user story 拆解、改動檔案清單、DTO / handler / helper 名、acceptance C-V 矩陣、R-Q、implement-time findings、Constitution Check 計數）—— 那些 `specs/<NNN>/` 已完整。

**精簡紀律**：本檔每 session 由 SOP hook 全檔注入 additional context，膨脹即是固定 context 稅。驗證證據（cargo test log、curl 輸出、psql 結果）→ spec.md / quickstart.md；feature 規格細節 → `specs/<NNN>/`；brainstorm 決策 → `docs/superpowers/`。本檔只留「現狀 + 衍生工作 + 完成索引 + 待驗證」四類精簡資訊。
