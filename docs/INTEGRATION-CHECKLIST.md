# INTEGRATION-CHECKLIST — rev1 整合進度追蹤

> 此檔 = 進度追蹤 + brainstorming 決策快照 + 跨 feature 待驗證項。
> 不放原則（→ `.specify/memory/constitution.md`）、不放規格細節（→ `specs/<NNN>/`）、不放操作參考事實（如預設帳號 → CLAUDE.md §5）。
> 每次 session SOP hook（`.claude/hook-git-submodule-SOP.sh`）自動 cat 本檔 head section 注入到 Claude session 第一輪 additional context。

---

## 🎯 Current Focus

**Phase**:**DESIGN-A 本體(F1–F12)收尾完成** — F12 `cleanup-job` 已落地(outer `78e585c` + merge `86e56e5`、rust-api `30c8dd4`、C-V1~C-V9 acceptance 9/9 PASS);DESIGN-A §6.1 的 F1–F12 application feature + Track DESIGN-A 部署三件套(W-FA1/W-FA2/W-FA3)+ Phase W deploy P1/P2/P4 皆完成,**DESIGN-A 本體全數收尾**。
**Active feature**:—(無進行中 feature;F12 已完成、兩段式 commit 落地、push + merge 待 user 同意)
**下一步**:DESIGN-A 本體(F1–F12)收尾後,後續候選(非 DESIGN-A 本體):F13 rust-refresh-token-impl + F14 design-a-to-b-cutover(§6.1 Phase 5 DESIGN-A→DESIGN-B 遷移、有「過渡橋 F10 在 DESIGN-A 形態下 N 週驗證」time gate)、W-F6b acme-cert-acquisition、observability W-F12/W-F13/W-F14(Phase W-5 P5)。

> **F5.1 階段同期(P2 主體解鎖)**:F5.1 base-web auth-login-and-dynamic-menu 已完成 merge(2026-05-15、outer `be6e237` + merge `e71aefe`、rust-api `a85e88c`),F5.2 Casbin redis pub-sub 與 F10/F14 同期。
>
> ✅ **W-F1 acceptance 階段發現 F5.1 pre-existing wiring bug — 已由 F7.1 修復**:`/route/getUserRoutes` 在 `sys_menu_route.rs:73` mount 但 handler `SysAuthenticationApi::get_user_routes` 期 `Extension<Arc<SysAuthService>>`、SysMenuRouter 原只用 SysMenuService 注入致 HTTP 500。F7.1 fix-route-getuserroutes-wiring US1 把 `init_protected_menu_router` 改 multi-service manual layer pattern 補上 `Arc<SysAuthService>` Extension layer、`/route/getUserRoutes` 對 3 role 全回 HTTP 200(見下方 F7.1 里程碑)。

---

## 已完成里程碑

- [x] outer git init + push（`miso168net/fork260509-rev1`、default branch `rev1-admin-root`）
- [x] worktree + submodule 配置（`base-web` / `rust-api` 雙重身分；CLAUDE.md §9 操作手冊）
- [x] spec-kit v0.8.7 + extensions（before_specify pre-hook → `speckit.git.feature`）
- [x] constitution v1.0.0（`.specify/memory/constitution.md`、Principle I-V）
- [x] **F4 response-shape-alignment** ✅（2026-05-12 完成；outer `3d357e5`、rust-api `82bbde5`；spec `specs/001-response-shape-alignment/`）
- [x] **F3 soft-delete-infrastructure** ✅（2026-05-14 完成；outer `6941788` + merge `0f1c5c3`、rust-api `2a65e2c`；spec `specs/002-soft-delete-infrastructure/`）
- [x] **F2.1 audit-log-infrastructure** ✅（2026-05-14 完成；outer `3c9c689` + merge `209a2c8`、rust-api `6bfa674`；spec `specs/003-audit-log-infrastructure/`；F2.2 outbox + Redis TTL 留後續）
- [x] **F1.1 jwt-secrets** ✅（2026-05-15 完成；outer `c582db4` + merge `5f82df3`、rust-api `65ce06a`；spec `specs/004-jwt-secrets/`；F1.2 algorithm 升級 + key versioning + refresh-token 預埋 留 with F10 refresh-token-bridge 同期）
- [x] **F5.1 auth-login-and-dynamic-menu** ✅(2026-05-15 完成;outer `be6e237` + merge `e71aefe`、rust-api `a85e88c`;spec `specs/005-auth-login-and-dynamic-menu/`;P2 第一個 feature 解鎖 base-web 主體;**2026-05-18 follow-up patch**:LoginInput field 加 `#[serde(alias = "userName")]` 對齊 base-web 既有 wiring — F5.1 acceptance 用 curl 驗 identifier work、但 browser SPA 走 base-web `fetchLogin(userName, password)` 送 `{userName, password}` 致 deserialize fail;1 行 alias 修、保 identifier 為 primary、base-web 零改動;CDP Edge 148 控制瀏覽器端到端登入流程 PASS(login → getUserInfo → /home + menuCount=43 dashboard 渲染完整);outer `1bdbc2f` + rust-api `1219418`)
- [x] **W-F1 dockerfile-rust-api** ✅(2026-05-15 完成;outer `bdbfb3c` + merge `430ada9`、rust-api `6831677`;spec `specs/006-dockerfile-rust-api/`;Phase W deploy P1 第一個 feature — debian+glibc multi-stage Dockerfile + /health endpoint + ENV APP_SERVER_PORT=11081 走 F1.1 env-override;acceptance 21/22 PASS、image 184MB、build 5m25s / cache hit 2s;1 partial = pre-existing F5.1 wiring bug 已記到 Current Focus)
- [x] **W-F2 dockerfile-base-web** ✅(2026-05-15 完成;outer `de1df10` + merge `ac79ed0`、base-web `cb897e9`;spec `specs/007-dockerfile-base-web/`;Phase W deploy P1 第二個 feature — node:22-slim builder + nginx:1.27-alpine runtime multi-stage Dockerfile + deploy/nginx.conf + .dockerignore 新建 3 個檔;corepack prepare pnpm@10.18.0 顯式 pin;Vite build-arg `VITE_SERVICE_BASE_URL=/api` 透過 process.env override .env.prod mock URL;nginx user pid permission 修;acceptance 12/12 + 4 remediation 全 PASS、image 24.3MB、build 65s cold / cache hit 4s)
- [x] **W-F3 compose-base-structure** ✅(2026-05-15 完成;outer `aa23840` + merge `04671d0`、base-web W-F2 followup `d56b9f8`;spec `specs/008-compose-base-structure/`;Phase W deploy P1 第三個 feature — 新建 outer repo root `docker-compose.yml` + `.env.example` + base-web Dockerfile W-F2 followup fix nginx cache subdir;5 service stack(postgres:17.4 / redis-stack:7.4.0-v3 / migration / rust-api / base-web)+ 1 internal network + 2 named volumes;Q1 postgres + Q2 redis 沿用 W-F1 verified、Q3 嚴守 W-F7 邊界不開 host port;acceptance 18/18 全 PASS、stack up-to-healthy 71s、/health p99 0.45ms、SC-001~SC-006 全達標)
- [x] **W-F4 secret-injection** ✅(2026-05-15 完成;outer `b3d027e` + merge `ab658d7`、rust-api `4057770` + `adf5f4c`;spec `specs/009-secret-injection/`;Phase W deploy P1 **最後一個**(4/4)— Docker secrets + `_FILE` pattern 升級 W-F3 過渡 secret 模式;5 secret entries(jwt_secret / database_url / redis_url / postgres_password / redis_password)+ 4 service secrets ref + rust-api 2 type-specific helper + 既有 callsite 並列加 call + EnvConfigLoader filter `APP_*_FILE` 解 config-rs `_` separator ambiguity(W-F4 implement 階段發現、R-001 amendment);acceptance 13/13 task / 18/18 scenario 全 PASS、4 long-running services healthy + migration exited 0、redis ps 完全不洩 password、/health rust-api+base-web 都 ok、image rebuild 3m51s。**Phase W deploy P1 達 100%、解鎖 P2**)
- [x] **W-F5 front-nginx** ✅(2026-05-16 完成;outer `101c9ac` + merge `dff14c2`;spec `specs/010-front-nginx/`;Phase W deploy **P2 第一個 feature**(1/3)— stack 內反向代理 + SPA gateway、解決 base-web `/api/` prefix vs rust-api root path mismatch;新增 `front-nginx` service(`nginx:1.27-alpine`、internal-only)+ `deploy/front-nginx/conf.d/default.conf`(2 upstream + 3 location + 5 header);5 routing path 全通(SPA `/` → base-web / `/api/auth/login` → rust-api 切前綴 200+JWT / `/api/nonexistent` 透傳 404 / SPA fallback / front-nginx self `/health`);`sys_login_log` 抓到真實 client IP `172.20.0.5` + `curl/7.88.1` UA(X-Forwarded-For 機制 work);6 service stack 起動正常、nginx -t syntax OK、無 ports/TLS/secrets/nestjs/rate limiting(嚴守邊界);**單段 commit**(只動 outer、不動 worktree)。**Phase W deploy P2 第一個 feature 達成、解鎖 W-F6 TLS + W-F7 對外 port**)
- [x] **W-F7 port-mapping** ✅(2026-05-17 完成;outer `ad239c4` + merge `62b3475`;spec `specs/011-port-mapping/`;Phase W deploy **P2 第二個 feature**(2/3)— 新增 outer-repo root `docker-compose.dev.yml` 拆檔(1 個新檔 ~22 行)+ 4 個 host port forward(`127.0.0.1:11080:80` front-nginx + `127.0.0.1:11081:11081` rust-api 直連 + `127.0.0.1:15432:5432` postgres + `127.0.0.1:16379:6379` redis),全綁 127.0.0.1 loopback;主 docker-compose.yml 嚴格不動(prod safe baseline);dev `docker compose -f -f up`、prod `docker compose up` 顯式切換;同步更新 CLAUDE.md §5.2 + INTEGRATION-CHECKLIST.md;US1 7/7 + US2 4/4 + US3 1/2 acceptance PASS(T036 SPA 瀏覽器 e2e + T051 LAN 跨機驗 deferred manual);**單段 commit**(只動 outer);dev/WSL 本機驗工作流解鎖、解鎖 W-F6 P2)
- [x] **W-F6 tls-cert-management** ✅(2026-05-18 完成;outer `38cd074` + merge `5e38030`;spec `specs/012-tls-cert-management/`;Phase W deploy **P2 第三個 feature**(3/3)— 加 TLS 終止 + 對外 port + prod cert lifecycle skeleton(11 個檔);主 compose 加 named volume front_nginx_certs + acme.sh service(profile=prod)+ acme_email secret(per W-F4 _FILE pattern)+ front-nginx cert mount(**不**加 ports、per Clarify Q1 避 binding 衝突);dev.yml 加 127.0.0.1:11443:443 + dev-certs mount(覆蓋 named volume);prod.yml 新建(0.0.0.0:11080+11443 + default.conf.prod mount override);nginx config 加 443 ssl server block(http2 on / TLSv1.2/1.3 / HIGH cipher) + 抽 snippets/proxy_headers.inc DRY;新建 default.conf.prod variant(80 redirect 443 + 保 /.well-known/acme-challenge/);新建 deploy/generate-dev-cert.sh(openssl RSA 4096 自簽、SAN DNS:localhost + IP:127.0.0.1、365 天);新建 deploy/dev-certs/ + deploy/secrets/acme_email.txt.example;.gitignore 加 dev-certs/*.pem;同步 CLAUDE.md §5.2 + INTEGRATION-CHECKLIST.md;US1 P1 MVP 7/7 + US2 P2 4/4 + US3 P3 4/4 = 15/15 acceptance PASS;cert SAN 含 DNS+IP 雙覆;TLSv1.3 + AES-256-GCM cipher 驗成;nginx -t syntax OK;dev/prod 3 mode 切換 work;**單段 commit**(只動 outer);Constitution Check 7 PASS / 14 N/A / 2 Partial(TLS — acme acquisition 留 W-F6b、Complexity Tracking 已合理化)/ 0 violation;解鎖 W-F6b 真實 cert acquisition)
- [x] **F6 route-guard** ✅(2026-05-18 完成;outer `b079248` + merge `a431215`、rust-api `1649aa9`;spec `specs/013-route-guard/`;application Phase 2 **第二個 feature**(F5.1 後)— 新增 rust-api 1 個 endpoint `GET /route/isRouteExist?routeName=<name>` 補完 base-web vue-router guard 在 dynamic auth route mode 下「not-found vs 無權限」disambiguation 邏輯;改 / 新建 8 個檔(5 core rust 改 + 1 migration 新建 + 2 plumbing register):`IsRouteExistInput` DTO(model/input/sys_menu.rs)+ `SysMenuService::is_route_exist` method(service)+ `SysMenuApi::is_route_exist` handler(api)+ route mount + RouteInfo(router)+ migration `m20260518_a_f6_is_route_exist_seed.rs`(snake_case 因 cargo `-D warnings`、Casbin INSERT 3 row 對 SUPER/ADMIN/USER allow)+ 2 個 mod re-export 改;SQL filter 用 `sys_menu::find_active()`(F3 helper 隱含 deleted_at IS NULL)+ `Status::Enabled` 2 個顯式 filter(合 spec FR-003 3 condition);**兩段式 commit**(per CLAUDE.md §6.1 — 動 rust-api worktree;此為 application Phase W 後第一個動 worktree 的 feature、有別於 W-F1~W-F6 純 outer 單段);acceptance US1 P1 MVP 4/4(home+manage_user+manage_role+manage_menu+log_login+access-key 全 true、3 個 non-seeded 全 false)+ US2 P2 4/4(nonexistent → false / empty → 200+false MVP 接受行為 / missing → HTTP 400 deserialize fail / auth fail → F4 envelope code 5001/3333 沿用 F5.1 既有 200+envelope pattern 非 HTTP 401)+ US3 P2 5/5(soft-delete → false → restore true / disabled → false → restore true / final 環境未污染)= **13/13 PASS**;Casbin allowlist mode 補 seed(plan R-2 新發現、F6 範疇內);避開 F5.1 既有 `init_protected_menu_router:73` wiring bug 模式(F6 用 `SysMenuApi::is_route_exist`、不是 `SysAuthenticationApi`);base-web src 零改動驗證 ✅(`git diff HEAD -- base-web/src/` empty、per Constitution Principle IV);Constitution Check 8 PASS / 15 N/A / 0 violation;解鎖 base-web vue-router guard dynamic auth route mode 完整體驗、F7 後續 menu CRUD 自動可被查、F11 demo/test role 403 重導行為依賴 F6)
- [x] **W-FA1 compose-nestjs-service** ✅(2026-05-18 完成;outer `b2c3ff7` + merge `b095d55`;spec `specs/014-compose-nestjs-service/`;Phase W deploy **P7 Track DESIGN-A 第一個 feature**(W-FA1/W-FA2/W-FA3 三件套之首)— 把 nestjs service 加進 rev1 docker compose stack、走 profile=track-a 啟動模式、共享 rev1 既有 postgres + redis、JWT secret 透過 W-F4 _FILE pattern 與 rust-api 共享;改 / 新建 6 個 outer file(`docker-compose.yml` 加 nestjs service block ~30 行 + secrets 加 refresh_token_secret / `docker-compose.dev.yml` 加 127.0.0.1:11082:9528 / `docker-compose.prod.yml` 加 nestjs 無 ports restart:always / `deploy/secrets/refresh_token_secret.txt.example` 新建 / `CLAUDE.md` §5.2/§5.2.1 加 track-a 模式 + port 11082 / `docs/INTEGRATION-CHECKLIST.md` 加 Phase W-7 roadmap + discoveries)+ nestjs:rev1-admin-nestjs image local build(870MB、未 push registry、留 W-FA3);**單段 commit**(per W-F* 慣例、只動 outer 不動 worktree);acceptance US1 P1 MVP 3/3 + US2 P1 3/3 + US3 P2 3/3 + US4 P2 3/3 + US5 P3 2/2 + US6 P2 3/3 = **17/17 PASS**;sys_tokens 表存在驗(rust-api 已 migrate)、F6 login regression PASS、base-web/rust-api/nestjs fork **三邊零改動**(per FR-018 + Constitution Principle IV/V 延伸);**2 個 implement-time amendments**:① NODE_VERSION build-arg override(`--build-arg NODE_VERSION=22.11.0` 解 pnpm 9.1.2 + Node 20.11.1 `ERR_UNKNOWN_BUILTIN_MODULE`、spec A-002 anticipated)/ ② healthcheck endpoint R-1 amendment(spec FR-008 原 `/v1/route/getConstantRoutes` 觸 nestjs prisma `public.Status` PG enum vs rust-api SMALLINT 致 PG 42704;改用 `/v1` root `@Public @BypassTransform getHello()` 不查 DB、保 PASS 與 schema 對齊無關;sys_menu prisma model rebuild 留 F10 範疇);Constitution Check 13 PASS / 16 N/A / 0 violation;DESIGN-A → DESIGN-B 遷移時整組刪除 3 處(service block / secret 條目 / host port)、無 DB / application 改動;解鎖 W-FA2 nginx-track-a-transitional-block + F10 refresh-token-nestjs-bridge + W-FA3 cicd-nestjs-build-job)
- [x] **W-FA3 cicd-nestjs-build-job** ✅(2026-05-18 完成;outer `68c5c4e` + merge `f23f38e`;spec `specs/016-cicd-nestjs-build-job/`;Phase W deploy **P7 Track DESIGN-A 第三個也是最後一個 feature**(W-FA1/W-FA2/W-FA3 三件套收尾、W-FA2 後接續、F10/F13/F14 之前)— 把 W-FA1 落定的 nestjs image build cmd 抽象成 local shell script `deploy/build-nestjs.sh`、自動化 build cmd + NODE_VERSION build-arg(22.11.0)內建 + `--tag <name>` 選項 + 結尾印 image size feedback;改 / 新建 3 個 outer file ~50 LOC(`deploy/build-nestjs.sh` 新建 28 行 bash + `CLAUDE.md` §5.2.1 line 165-172 inline build cmd 換成 1 行 `bash deploy/build-nestjs.sh` + `docs/INTEGRATION-CHECKLIST.md` Phase W-7 W-FA3 row + Current Focus + 已完成里程碑);**單段 commit**(per W-F* / W-FA* 慣例、只動 outer 不動 worktree、不動 nestjs fork);acceptance US1 P1 MVP 3/3 + US2 P2 1/1 + US3 P3 2/2 + US4 P2 2/2 = **8/8 PASS**;base-web/rust-api/nestjs fork **三邊零改動**(per FR-013 + Constitution Principle IV/V 延伸);W-FA1 stack 7 service expected state(per US1.3 / US4.2、6 service healthy + migration init-container exited 0);W-FA2 refreshToken endpoint HTTP 404 + nestjs envelope `{"code":404,"message":"Refresh token not found."}`(per US4.2、走通 nginx → nestjs 業務邏輯、wire-up 不退化);LOC = 28 ≤ 30(per NFR-001 / SC-007);cold build ~9s + idempotent run ~7s(13 step CACHED hit、per SC-003 < 30s);`--tag rev1-test` 落地獨立 image SHA + 不影響 `:rev1-admin-nestjs` baseline(per SC-004 / C-S8);**1 個 implement-time amendment**:plan T020 template 與 spec contract C-S3/C-S6 寫 `nestjs:rev1-admin-nestjs:${TAG}` 為 docker 無效 tag 格式(第一個 `:` 為 repo/tag 分隔、tag 內不能含 `:`)— 改用 `nestjs:${TAG}` 配 TAG default=`rev1-admin-nestjs`(對齊 W-FA1 compose `image: nestjs:rev1-admin-nestjs`),`--tag rev1-test` 產出 `nestjs:rev1-test` 獨立變體;output `[W-FA3] Built nestjs:<tag> (<size>)`;同步壓縮至 28 LOC 對齊 NFR-001(spec/plan 表寫 ≤ 30、template 自身為 38 LOC、移除 blank line + 合併 case branch 後達成);F14 cutover 時 `rm deploy/build-nestjs.sh` + 改回 CLAUDE.md §5.2.1 inline cmd(or 改其他 build pipeline)即可、無 DB / application 改動;Track DESIGN-A 三件套(W-FA1+W-FA2+W-FA3)**deploy 結構 wire-up 完整**、下一步 F10 refresh-token-nestjs-bridge;Constitution Check 6 PASS / 17 N/A / 0 violation;解鎖 F10 + F13 + F14、Phase W deploy P7 Track DESIGN-A 全部完成)
- [x] **W-FA2 nginx-track-a-transitional-block** ✅(2026-05-18 完成;outer `e8f4dac` + merge `c5b7840`;spec `specs/015-nginx-track-a-transitional-block/`;Phase W deploy **P7 Track DESIGN-A 第二個 feature**(W-FA1/W-FA2/W-FA3 三件套第二個、W-FA1 後接續、F10/F13/F14 之前)— 把 `POST /api/auth/refreshToken` 路由補上 nginx 反代到 nestjs upstream、用 inline TRANSITIONAL marker block(per DESIGN-A §2.2 convention)+ variable proxy_pass + resolver lazy DNS 設計(default profile / DESIGN-B 退場 nginx 仍可啟動);改動範圍極緊湊(4 個 outer file ~52 LOC:`deploy/front-nginx/conf.d/default.conf` dev 80 + dev 443 兩 server block 各加 ~9 行 inline TRANSITIONAL block + `deploy/front-nginx/conf.d/default.conf.prod` 443 server block 加 ~9 行(prod 80 只 redirect、不加)+ `CLAUDE.md` §5.2.1 加 track-a refreshToken curl 範例 + `docs/INTEGRATION-CHECKLIST.md` Phase W-7 W-FA2 row + Current Focus + 已完成里程碑);**單段 commit**(per W-F* 慣例、只動 outer 不動 worktree、不動 nestjs fork);acceptance US1 P1 MVP 3/3 + US2 P2 3/3 + US3 P2 3/3 + US4 P2 3/3 = **12/12 PASS**;base-web/rust-api/nestjs fork **三邊零改動**(per FR-013 + Constitution Principle IV/V 延伸);default profile 下 nginx -t 仍 OK、refreshToken 回 HTTP 502 不 crash(per FR-017 + Q2 lazy DNS 設計、front-nginx 仍 healthy);prod HTTPS refreshToken 走通 nginx → nestjs 對齊 dev wire-up(nestjs log 雙證);F14 cutover dry-run sed 機械刪除 marker block 後 nginx -t exit 0(per SC-005、證 F14 cutover 路徑乾淨);**1 個 implement-time observation**:T030 / SC-001 spec acceptance 寫「HTTP status code 不是 404」但實際上 nestjs business logic 也會回 404(`NotFoundException: Refresh token not found.` from `AuthenticationService.refreshToken`、token 不在 DB)— wire-up 完整通過 nginx → nestjs(nestjs log + front-nginx access log + rust-api log 三證,rust-api 完全無 hit),HTTP 404 來自 nestjs 業務邏輯而非 rust-api 攔截;對齊 spec「W-FA2 不負責 business 正確性、留 F10」、應視為 spec 描述細節(SC-001 強條件「不是 404」嚴格解讀不通過、但其底層 intent「不被 rust-api 攔」完全通過);Constitution Check 8 PASS / 15 N/A / 0 violation;DESIGN-A → DESIGN-B 遷移時整組 `sed '/>>>>> TRANSITIONAL BEGIN/,/<<<<< TRANSITIONAL END/d'` 刪除 3 處 marker block + 改 `proxy_pass` 指 `rust_api` 即可、無 DB / application 改動;解鎖 F10 refresh-token-nestjs-bridge + W-FA3 cicd-nestjs-build-job + F13 rust-refresh-token-impl + F14 design-a-to-b-cutover)
- [x] **F7.1 fix-route-getuserroutes-wiring** ✅(2026-05-20 完成;outer `554436f` + merge `efe910e`、rust-api `4de171a`;spec `specs/023-fix-route-getuserroutes-wiring/`;application Phase 3 **F7 manage-crud-alignment merge `136b1eb` 後第一個 post-F7 follow-up feature** — F7 CDP browser smoke demo(由 F7 acceptance C-V10 deferred 改實際跑)過程 catch 出 2 個 backend acceptance gap 修齊、性質都是 wiring/shape 漏修:**US1 P1 F5.1 wiring bug fix** — `init_protected_menu_router()` 從 `merge_router!` 單 service 注入改 multi-service manual layer pattern(對齊 F9 R-③ + 既有 `init_authorization_router` pattern)、`.layer(Extension(Arc<SysMenuService>))` + `.layer(Extension(Arc<SysAuthService>))` + `apply_layers(Services::None, true, true, ...)`、解 `/route/getUserRoutes` HTTP 500 `Missing request extension`;**US2 P2 menu paginated wrapper fix** — `list_menu_for_systemmanage` return type `Res<Vec<SystemManageMenuOutput>>` 改 `Res<PaginatedData<SystemManageMenuOutput>>`、`current=1` / `size=total=records.len()`(rust 端不分頁、全集當 1 頁)、對齊 base-web TS type `Api.SystemManage.MenuList = Common.PaginatingQueryRecord<Menu>`、解 base manage/menu view「无数据」;改動範圍 rust-api **2 file ~25 LOC**(`server/initialize/src/router_initialization.rs` ~15 LOC US1 + `server/api/src/admin/sys_system_manage_api.rs` ~10 LOC US2、F7 既有 4 個其他 systemManage handler + `SystemManageMenuOutput` Output DTO 全不動);**兩段式 commit**(per CLAUDE.md §6.1 + F5.1/F6/F10.1/F10.2/F11/F9/F7 慣例、動 rust-api worktree、**無 docker-compose.yml 改**、**無 migration 改**、對比 F7 有 migration);acceptance C-V1~C-V10 = **10/10 PASS**(C-V1 image rebuild OK 新 SHA `9cf4c6c` / C-V2 Soybean `/route/getUserRoutes` HTTP 200 + envelope code:0 + routes len=4 [home/log/access-key/manage] + manage 4 children [manage_user/manage_role/manage_menu/manage_user-detail] / C-V3 Administrator HTTP 200 + code:0 + routes 2 / C-V4 GeneralUser HTTP 200 + code:0 + routes 1 — 非 HTTP 500 / C-V5 `getMenuList/v2` envelope `data` 4 key `{current:1, size:9, total:9, records}` + records[0] 22 field menuType "2" / parentId String / buttons null / C-V6 CDP smoke `/manage/menu` 9 row 不再「无数据」 / C-V7 `/manage/user` 5 row regression PASS / C-V8 `/manage/role` 3 row regression PASS / C-V9 三邊 scope:base-web src 0 diff + nestjs fork 0 diff + rust-api 2 file ~25 LOC + 0 docker-compose diff + 0 migration diff / C-V10 W-FA1 stack 6 service healthy + rust-api 剛 recreated + migration exited 0);無 rust unit test(per spec FR-015 + F7 FR-021 同精神、wiring/wrapper 邏輯 stack-可見、curl + CDP smoke 驗即可);CDP smoke 沿用 F7 R-Q4 setup(WSL2 host Edge 148 + `--remote-debugging-port=9229` + `Fetch.requestPaused` 攔 `/auth/getUserInfo` 注入 R_SUPER/R_ALIAS demo-only workaround);base-web + nestjs fork **三邊零改動**(per FR-010 + FR-011);Constitution Check 3 PASS / 1 N/A(Principle II soft-delete/audit — F7.1 無 DB 寫入)/ 0 violation;subagent-driven-development + 兩段式 spec/code review 執行;**解 W-F1 acceptance 階段發現的 F5.1 pre-existing wiring bug**(見上方 Current Focus ✅ 註)、解 F7 acceptance C-V10 deferred 的 CDP smoke baseline、解鎖 F8 assign-users;role code mismatch(`R_SUPER`/`R_ADMIN` vs `ROLE_*`)留 F7.2 role-code-alignment follow-up)
- [x] **F7.2 role-code-alignment** ✅(2026-05-20 完成;outer `68410df` + merge `476ca88`、rust-api `84cbc19`;spec `specs/024-role-code-alignment/`;application Phase 3 **F7.1 fix-route-getuserroutes-wiring merge `efe910e` 後第一個 follow-up feature** — F7.1 CDP browser smoke demo 為讓 base-web example 分支 static 模式路由過濾通過、用了 `Fetch.requestPaused` 攔 `/auth/getUserInfo` 注入 `R_SUPER`/`R_ADMIN` role alias 的 demo-only workaround;F7.2 收掉 workaround:**US1 P1 getUserInfo role code alias 映射** — `server/api/src/admin/sys_authentication_api.rs` 加 `map_role_alias` 純函式(明確 3-entry `match`:`ROLE_SUPER`→`R_SUPER` / `ROLE_ADMIN`→`R_ADMIN` / `ROLE_USER`→`R_USER`、未知 code 原樣 pass through、**不用** `ROLE_`→`R_` prefix transform)+ `get_user_info` handler `roles` 套 `map_role_alias` + `#[cfg(test)] mod tests` 含 `test_map_role_alias`(3 known + 1 unknown);改動範圍 rust-api **1 file ~10 LOC core + 1 unit test**(`sys_authentication_api.rs`、F11 既有 3 stub handler + F5.1 既有 4 handler + `UserInfoOutput` struct 全不動);**兩段式 commit**(per CLAUDE.md §6.1 + F5.1/F6/F10.x/F11/F9/F7/F7.1 慣例、動 rust-api worktree、**無 docker-compose.yml 改**、**無 migration 改**);acceptance C-V1~C-V9 = **9/9 PASS**(C-V1 image rebuild OK 新 SHA `963aa9b` / C-V2 Soybean `/auth/getUserInfo` `data.roles=["R_SUPER"]` / C-V3 Administrator `["R_ADMIN"]` / C-V4 GeneralUser `["R_USER"]` — 皆不含 `ROLE_*` / C-V5 CDP smoke 3 view **不帶** role alias workaround 原生 render:`/manage/menu` 9 row + `/manage/user` 5 row + `/manage/role` 3 row、皆非「无数据」 / C-V6 Casbin enforce regression 三 user `/route/getUserRoutes` HTTP 200 + envelope code:0、enforce 路徑維持 `ROLE_*` 不退化 / C-V7 rust unit test `test_map_role_alias` ok(`rust:1.86-slim-bookworm` 容器內 host-mounted worktree 跑、host 無 cargo)/ C-V8 三邊 scope:base-web src 0 diff + nestjs fork 0 diff + rust-api 1 file + 0 docker-compose diff + 0 migration diff / C-V9 W-FA1 stack 6 service healthy + rust-api 剛 recreated);無 docker 階段編譯 `#[cfg(test)]`(`cargo build` 不含 test cfg、unit test 改於 rust 容器 `cargo test` 驗);**1 個 implement-time finding**:rust 容器內 `cargo test` 順帶把 repo 內 stale `Cargo.lock` 同步(`chrono`/`serde_json`/`tracing` dep 早已被使用但 lock 未記、pre-existing drift、非 F7.2 引入)— `git checkout Cargo.lock` 還原保 F7.2 scope 嚴守 1 file;base-web + nestjs fork **三邊零改動**(per FR-008 + FR-009);JWT `Claims.role` / `casbin_rule.v0` / `sys_role.code` DB 三者全不動、映射只在 `getUserInfo` response 邊界;Constitution Check 4 PASS / 1 N/A(Principle II soft-delete/audit — F7.2 無 DB 寫入)/ 0 violation;subagent-driven-development + 兩段式 spec/code review 執行;**消除 F7.1 CDP role alias workaround**、base manage/* CDP smoke 原生通、解鎖 F8 assign-users)
- [x] **F8 assign-users** ✅(2026-05-20 完成;outer `f1bfff7` + merge `c2b0912`、rust-api `90f37d1`;spec `specs/025-assign-users/`;application Phase 3 **第三個也是最後一個 feature** — F7/F7.1/F7.2 把 base manage/* 後台讀路徑與 role code 對齊跑通後,F8 補上唯一缺的 user-role 寫路徑:**US1 P1 assign-users user-role 指派 endpoint** — `server/api/src/admin/sys_authentication_api.rs` 加 `SysAuthenticationApi::assign_users` handler(比照 sibling `assign_routes`、`Extension<Arc<SysAuthorizationService>>` + `ValidatedForm<AssignUserDto>` → `service.assign_users(role_id, user_ids)` → `Res::new_data(())`、**無 enforcer**)+ `AssignUserDto` import;`server/router/src/admin/sys_authentication_route.rs` `init_authorization_router` 加 `POST /authorization/assign-users` route mount + `RouteInfo`;新建 `migration/src/datas/m20260522_a_f8_assign_users_seed.rs` Casbin `p` policy seed 1 row(`ROLE_SUPER` × `/authorization/assign-users` × POST、`v4=''`)+ `datas/mod.rs` + `lib.rs` 註冊;改動範圍 rust-api **5 file ~35 LOC**(2 改 source + 1 新建 migration + 2 register、既有 `AssignUserDto` + `assign_users` trait/service impl + 其他 handler 全不動);user→role 只寫 `sys_user_role` join table(整組覆蓋語意)、不寫 Casbin `g` rule(per brainstorm Q2);只 ROLE_SUPER 可呼叫(對齊 sibling `assign-permission`/`assign-routes`);**兩段式 commit**(per CLAUDE.md §6.1 + F5.1/F6/F10.x/F11/F9/F7/F7.1/F7.2 慣例、動 rust-api worktree、**無 docker-compose.yml 改**、**有 1 個 Casbin seed migration**、無 DB schema 改);acceptance C-V1~C-V12 = **12/12 PASS**(C-V1 image rebuild OK 新 SHA `1db53a9` / C-V2 migration init-container apply `m20260522` exited 0 / C-V3 Casbin policy row 落 DB 1 row `p|ROLE_SUPER|built-in|/authorization/assign-users|POST` / C-V4 Soybean(ROLE_SUPER)`POST /authorization/assign-users` `{roleId:3,userIds:[3,2]}` HTTP 200 + code:0 / C-V5 `sys_user_role` role 3 反映 `{2,3}` / C-V6 Administrator re-login `getUserInfo` `data.roles=["R_ADMIN","R_USER"]`(經 F7.2 alias)/ C-V7 restore `{roleId:3,userIds:[3]}` HTTP 200 + code:0 + seed 回 baseline 無污染 / C-V8 Administrator + GeneralUser deny envelope `code:5001` 2/2 + 未寫入 / C-V9 sibling `assign-permission`/`assign-routes` 非 404 regression / C-V10 三邊 scope:base-web 0 diff + nestjs fork 0 diff + rust-api 5 file + 0 docker-compose diff / C-V11 W-FA1 stack 6 service healthy + rust-api 剛 recreated / C-V12 空 `userIds` / 空 `roleId` validation rejection envelope `code:400`(非 code:0)+ `sys_user_role` 無變動);無 rust unit test(per spec FR-014、F8 純 HTTP wiring、無可獨立測之 pure function、同 F7.1 wiring precedent);base-web + nestjs fork **三邊零改動**(per FR-010 + FR-011);Constitution Check 5 PASS / 0 N/A / 0 violation;subagent-driven-development + 兩段式 spec/code review 執行;**收尾 application Phase 3 至 3/3**(F9 + F7 + F8)、補齊 rust-api `/authorization/assign-*` 三件套(assign-permission / assign-routes / assign-users))
- [x] **W-F11 rust-horizontal-scaling** ✅(2026-05-21 完成;outer `d02c7c9` + merge `d2d4c4c`、rust-api `d122b23`;spec `specs/026-rust-horizontal-scaling/`;Phase W deploy **Phase W-4 (P4) 第一個 feature** — application Phase 3 收尾 3/3(F8)後第一個 Phase W deploy feature、rust-api 水平擴展能力;三塊互相依賴的工作一次交付:**(A) Casbin redis pub-sub 跨 instance 一致性** — channel `casbin:policy:invalidate`、publisher(`notify_casbin_changed()` in `server_global`)+ subscriber 背景 task(`casbin_sync_initialization.rs` in `server_initialize`)、policy 異動 publish、收訊息 `load_policy()` full reload + `CachedEnforcer` cache clear / **(B) compose 多 replica** — `docker-compose.prod.yml` rust-api `deploy.replicas: 2`、dev 維持單實例 / **(C) nginx upstream auto-discovery** — `default.conf.prod` `rust_api` upstream 改 resolver-based;**無 migration、無 DB schema 改**;**兩段式 commit**(per CLAUDE.md §6.1 + F5.1/F6/F10.x/F11/F9/F7/F7.1/F7.2/F8 慣例、動 rust-api worktree、有 outer 部署配置改(compose + nginx prod conf));acceptance C-V1~C-V9 = **9 個 C-V contract**(curl + psql + docker compose exec);base-web + nestjs fork **兩邊零改動**;**Phase W deploy Phase W-4 (P4) 第一個 feature**、application Phase 3 收尾(F8)後第一個 Phase W deploy feature;observability(W-F12/W-F13/W-F14、Phase W-5 P5)為後續候選)
- [x] **F12 cleanup-job** ✅(2026-05-21 完成;outer `78e585c` + merge `86e56e5`、rust-api `30c8dd4`;spec `specs/027-cleanup-job/`;application **DESIGN-A 本體(F1–F12)收尾** — §6.1 Phase 4(P4)最後一個 application feature;F3 給 7 張 admin 表加 `deleted_at` 軟刪、軟刪 row 永遠留表 → F12 提供獨立一次性 `cleanup` binary 定期物理清除過期軟刪 row;**三塊交付**:(A) rust workspace 新 crate `server/cleanup`(第 3 個 binary、複用 rust-api image、比照 migration)— `main.rs` 讀 `CLEANUP_RETENTION_DAYS` env(預設 90、無效拒跑 exit≠0)算 cutoff、掃 7 張軟刪表 `deleted_at < cutoff` 的 row、dry-run 預設只報表、`--execute` 才物理刪;每筆 per-row txn 內 `write_in_txn`(`HardDelete` / `Actor::system("cleanup_job")` / `Cleanup` source、`payload_before=audit_snapshot`)+ `delete_many` 原子、log-and-continue、`rows_affected==0` rollback 兜底 / (B) `docker-compose.yml` 加 `cleanup` service(`profiles: ["jobs"]`、entrypoint sh wrapper 注 `DATABASE_URL` + 透傳 `"$@"`、host cron `docker compose run --rm cleanup --execute` 觸發)+ `cleanup_database_url` secret / (C) `deploy/cleanup/setup-role.sql` — `cleanup_job` 最小權限 PG role(7 表 SELECT/DELETE + `sys_operation_log` SELECT/INSERT、idempotent);改動範圍 rust-api **新 crate `server/cleanup`(`Cargo.toml` + `src/main.rs` ~280 LOC 含 11 unit test)+ workspace `Cargo.toml` member + `Dockerfile` 3-binary** + outer **`docker-compose.yml` + `deploy/cleanup/setup-role.sql`**;**無 migration、無 DB schema 改、不碰 `casbin_rule`**;**兩段式 commit**(per CLAUDE.md §6.1 + F8/W-F11 慣例、動 rust-api worktree、有 `docker-compose.yml` 改);acceptance C-V1~C-V9 = **9/9 PASS**(C-V1 image 3 binary cleanup/server/migration / C-V2 role setup idempotent + 最小權限 `t|t`/`f|f` / C-V3 dry-run 報 1 row 不刪不寫 / C-V4 `--execute` 物理刪 + HARD_DELETE audit `cleanup_job`/`_system`/`module_name=sys_endpoint`/payload_before 非 null / C-V5 未到期 row 不刪 / C-V6 idempotent 重跑 sys_operation_log 217→217 / C-V7 retention `abc`/`-5`/`0` 三者皆 exit≠0 拒跑不碰資料 / C-V8 cleanup service profile-gated 不自動起 + 既有 stack 全 healthy / C-V9 三邊 scope:base-web 0 diff + nestjs fork 0 diff + dev/prod compose 0 diff + migration 0 diff);base-web + nestjs fork **三邊零改動**;**11 個純函式 rust unit test**(retention parse/validate + cutoff 計算 + dry-run 報表格式、`rust:1.86-slim-bookworm` 容器內 `cargo test` 跑、host 無 cargo);**2 個 implement-time finding 文件化**:① code-review catch `delete_many` 未檢 `rows_affected` — 並行刪除下會留 ghost `HARD_DELETE` audit row;修為 `rows_affected==0` 則 rollback + `warn` + 計入 `failed`(7 個 sweep block 一致套用)/ ② **setup-role.sql 偏離 data-model E6 / research R-Q5**:spec 設計的 `DO $$ ... CREATE ROLE ... PASSWORD :'cleanup_pw' ... $$` block 不可行 — 實測 psql 不在 `DO $$ $$` dollar-quoted block 內做變數插值(`syntax error at or near ":"`);改用頂層 `SELECT ... WHERE NOT EXISTS \gexec` 條件建 role + `\if :{?cleanup_pw}` gate 的頂層 `ALTER ROLE ... PASSWORD :cleanup_pw`(bare `:cleanup_pw`、ops 帶引號值 `-v cleanup_pw="'<pw>'"`)、idempotent 重跑不帶變數則跳過密碼設定;Constitution Check 5 PASS / 0 N/A / 0 violation;subagent-driven-development + 兩段式 spec/code review 執行;**DESIGN-A 本體(F1–F12)全數收尾** — §6.1 application feature 全落地,後續為 DESIGN-A→B 遷移 F13/F14 + observability W-F12~W-F14)
- [x] **F7 manage-crud-alignment** ✅(2026-05-20 完成;outer `44c7424` + merge `136b1eb`、rust-api `11b888c`;spec `specs/022-manage-crud-alignment/`;application **Phase 3 第二個 feature**(F9 已落、F7 為第二個、F8 待)、DESIGN-A §6.1 + DESIGN-B §6「manage/* 4 module CRUD shape 對齊 + Casbin enforce + 軟刪 + audit + Menu CRUD」針對 base example 分支 stub UI 現況精化定義為「base view shape 對齊 + admin path Casbin 補位」two-fold deliverable — Shape mapping:5 個 Output DTO + From impl + camelCase rename + 缺欄位 hardcode None/vec![];F9 alias 5 條 read mount 換 wrapper handler / Admin path Casbin allow:新 migration m20260521 INSERT 15 row 補 ROLE_ADMIN 對既有 /user/* /role/* /route/* path 解 A-006;改動範圍 rust-api **9 file ~315 LOC**(6 修 + 3 新建、超 spec SC-018 8 file 1 額外 server/model/Cargo.toml 加 tracing workspace dep、per spec FR-005 + R-Q2 map_icon_type unexpected default arm tracing::warn! 需要):`server/model/src/admin/output/sys_system_manage.rs` 新建 ~205 LOC(5 Output DTO + 4 From impl + map_menu_type/map_icon_type helper)/ `server/model/src/admin/output/mod.rs` +5 LOC mod + re-export 5 DTO / `server/model/Cargo.toml` +1 LOC tracing workspace dep / `server/api/src/admin/sys_system_manage_api.rs` 新建 ~74 LOC(SysSystemManageApi + 5 wrapper handler、call F9 既有 service method、用 .into() map raw model → Output DTO)/ `server/api/src/admin/mod.rs` +2 LOC mod + re-export SysSystemManageApi / `server/router/src/admin/sys_system_manage_route.rs` ~27 LOC(5 條 read alias mount 換新 wrapper handler + 1 import 改、SysRoleApi import 移除因 5 read 全換 handler 不再 reference)/ `migration/src/datas/m20260521_a_f7_admin_role_existing_paths_seed.rs` 新建 ~65 LOC(INSERT 15 row + scope-limited DELETE)/ `migration/src/datas/mod.rs` +1 LOC / `migration/src/lib.rs` +2 LOC Migrator vec 加;**兩段式 commit**(per CLAUDE.md §6.1 + F5.1/F6/F10.1/F10.2/F11/F9 慣例、動 rust-api worktree、**無 docker-compose.yml 改**、對比 F10.1);acceptance US1 P1 MVP 4/5(C-V1 image rebuild OK 新 SHA + 2 implement-time fix:server-api 不依賴 server-model crate 改 import 走 server_service::admin re-export + SysRoleApi unused import 移除 / C-V2 m20260521 + 15 row 落 DB + 17 total 含 F5.1+F6 baseline 2 row pre-existing `/route/getUserRoutes` + `/route/isRouteExist`、不阻 F7 / C-V3 Soybean 5 read alias shape 對齊 5 sub-case 全 PASS — 各 field 對齊 base TS type、缺欄位 hardcode null/[]、menuType "1"/"2" 而非 "menu"/"directory"、pId 大寫 I rename / **C-V10 CDP smoke deferred** 為 user manual visual eyeball per R-Q4 + A-013 graceful degradation、Edge debug port 不 reachable、backend shape end-to-end 已由 C-V3 verify)+ US2 P2 2/2(C-V4 Administrator 5 read alias 全 envelope code:0 / C-V5 Administrator 既有 5 path 5/5 envelope code:0 — 解 A-006、需 implement-time spec drift fix R-Q-AT1)+ US3 P3 3/3(C-V6 Menu CRUD admin path POST/PUT/DELETE 3/3 envelope code:0 + soft delete sys_menu.deleted_at NOT NULL + audit sys_operation_log 3 row(INSERT/UPDATE/SOFT_DELETE)、繼承既有 service hook per FR-020 / C-V7 GeneralUser deny 2/2 HTTP 200 + envelope {code:5001, success:false} per F11 R-Q6 envelope wrap / C-V9 W-FA1 stack 6 service healthy + rust-api 剛 restart + migration exited 0)+ zero-regression 1/1(C-V8 三邊 scope:base-web src 0 diff + nestjs fork 0 diff + rust-api 9 file ~315 LOC + 0 docker-compose diff + outer 4 file 1 untracked dir)= **10/11 PASS + 1 deferred**(對齊 spec FR-023 NFR-004、無 unit test per FR-021、繼承既有 service audit hook per FR-020);**3 個 implement-time finding 文件化**:① server-api crate 不依賴 server-model crate、`server_model::admin::*` 直接 import 不通、改走 `server_service::admin::*` re-export(server_service::admin::mod.rs 已 `pub use server_model::admin::{input::*, output::*}`)/ ② SysRoleApi unused import:F7 把 F9 既有 `/getRoleList` `/getAllRoles` 2 條 mount 換成 SysSystemManageApi、SysRoleApi 不再 reference、cargo `-D warnings` 觸 fail、1 LOC remove(F9 R-① unused import lesson 沿用)/ ③ **R-Q-AT1 spec drift refinement**:spec FR-012 + data-model E8 + migration source 寫 base path `/user/` `/role/` `/route/` (trailing slash)、實際 axum router mount + m20241024 ROLE_SUPER baseline 用 `/user` `/role` `/route` (無 trailing slash)、Casbin path string mismatch 致 C-V5a/b/c initial 5001 deny、acceptance 階段 catch + 修齊 migration source(8 row trailing slash 移除)+ 即時 in-DB UPDATE 8 row 對齊、C-V5 重跑 5/5 envelope code:0 + 1 implement-time spec correction(F11 R-Q5/R-Q6 + F9 R-Q1 + F7 R-Q1 之後第 5 個);base-web + nestjs fork **三邊零改動**(per FR-015 + FR-016 + SC-014 + SC-015);W-FA1 stack 6 service healthy + migration exited 0 維持;rust-api image rebuild 2m 30s warm cache(NFR-005 budget ≤ 5 min);Constitution Check 25 PASS / 7 N/A / 0 violation;Brainstorm 4 顯式 Q 全拍板(Q1 scope A read-only path + endpoint shape 對齊 / Q2 mapping A 最小 mapping + 缺欄位 hardcode / Q3 Casbin admin row A 補 15 row 新 migration m20260521 解 A-006 / Q4 acceptance A curl + psql + CDP browser smoke test);**解 spec A-006 已知差異**(F9 baseline):F7 m20260521 補 ROLE_ADMIN 對 `/user/* /role/* /route/*` 既有 path 的 Casbin allow row、Administrator 用既有 path 與 alias path 兩條都通;**Application Phase 3 進度 2/3 完成**:F9(systemManage-alias-router、10 條 alias 完整交付)+ F7(manage-crud-alignment、5 read alias shape 對齊 + admin path Casbin 補位)= 2/3 完成、F8 assign-users 待;F7 解鎖 F8 + base manage/* 4 view 跑通(3 view + admin path、not 4 view + UI CRUD per Q1)、並行可選 F12 / W-F11 / W-F6b)
- [x] **F9 systemManage-alias-router** ✅(2026-05-20 完成;outer `260f2c6` + merge `b2f910c`、rust-api `b744c8e`;spec `specs/021-systemmanage-alias-router/`;application Phase 4 收尾後 + F11 後第二個 post-Phase-4 feature、**DESIGN-A §4.2 抽離項清單收尾 feature**(F11 4 條 stub + F9 batchDeleteUser 1 條 = **5/5 完整交付**)、F12 之前可並行 — RESEARCH §6.2 方案 B「rust 加 /systemManage/* alias router」直譯實作、補 10 條 alias endpoint(5 重用 mount + 1 重用 method 變 updateUser POST mount per R-Q1 既有 PUT handler method-agnostic、不需新做 wrapper / 1 變形 wrapper deleteUser body 抽 id / 1 新做 stub batchDeleteUser per-row loop + counter / 2 新做完整 getAllRoles + getAllPages)+ 1 個 Casbin policy seed migration(`m20260520_a_f9_system_manage_alias_seed.rs` INSERT 20 row、Soybean ROLE_SUPER + Administrator ROLE_ADMIN allow、GeneralUser default deny per F11 R-Q5 v4='' baseline);改動範圍 rust-api **13 file ~305 LOC**(11 修 + 2 新建、超 spec SC-015 12 file 1 額外 input/mod.rs explicit re-export 對齊 transitive structure):`server/api/src/admin/sys_user_api.rs` 加 2 handler delete_user_by_body + batch_delete_users / `server/api/src/admin/sys_role_api.rs` 加 1 handler get_all_roles / `server/api/src/admin/sys_menu_api.rs` 加 1 handler get_all_pages / `server/service/src/admin/sys_role_service.rs` +trait + impl find_all_enabled / `server/service/src/admin/sys_menu_service.rs` +trait + impl find_all_page_keys / `server/model/src/admin/input/sys_user.rs` 加 2 DTO DeleteUserByBodyInput + BatchDeleteUserInput / `server/model/src/admin/input/mod.rs` re-export 2 DTO / `server/router/src/admin/sys_system_manage_route.rs` 新建 SysSystemManageRouter + 10 條 route mount + 10 RouteInfo / `server/router/src/admin/mod.rs` +mod + re-export / `server/initialize/src/router_initialization.rs` +import + 多 service Extension layer + apply_layers(per R-Q1 update_user 直接 mount POST 不需新做 update_user_post wrapper 修正 + 多 service init pattern 對齊既有 init_authorization_router、不用 merge_router! 因 macro 限單 service 注入)/ `migration/src/datas/m20260520_a_f9_system_manage_alias_seed.rs` 新建 + `migration/src/datas/mod.rs` register + `migration/src/lib.rs` Migrator vec 加;**兩段式 commit**(per CLAUDE.md §6.1 + F5.1/F6/F10.1/F10.2/F11 慣例、動 rust-api worktree、**無 docker-compose.yml 改**、對比 F10.1);acceptance US1 P1 MVP 5/5(C-V1 image rebuild OK 新 SHA + cargo `-D warnings` 觸 EntityTrait unused import fix 1 LOC / C-V3 Soybean 5 重用 mount 4 sub-case 全 HTTP 200 + 預期 paginated/tree shape 對齊 / C-V4 Soybean 4 變形+新做 endpoint:addUser 創建新 user + updateUser POST envelope wrap path 對齊既有 ValidatedForm + deleteUser body envelope code 6001 non-existent + batchDeleteUser HTTP 200 + envelope `{code:0, data: {deletedCount:1}}` partial counted per Q3 / C-V5 Soybean 2 新做完整 getAllRoles 3 role array(SUPER+ADMIN+USER 全 enabled)+ getAllPages 15 page name distinct route_name array per R-Q5 RouteName 對齊既有 is_route_exist / C-V7 audit log sys_operation_log row=SOFT_DELETE entity_id=batchDelete test user、繼承既有 service hook per FR-016)+ US2 P2 1/1(C-V6 GeneralUser getUserList HTTP 200 + envelope `{code:5001, success:false, msg:"您没有访问该资源的权限"}` per F11 R-Q6 envelope wrap)+ US3 P3 3/3(C-V2 psql casbin_rule 20 row 落 DB、v0×v2×v3 對齊期望、v4 全空字串 per F11 R-Q5 / C-V9 W-FA1 stack 6 service healthy + rust-api 剛 recreated 3 min + migration init container exited 0 / C-V10 既有 `/api/user` `/api/role` `/api/route/tree` 3 條 endpoint HTTP 200 + envelope 對齊 F9 落地前 — spec acceptance URL 帶 trailing slash + `?page=` query param 細節錯誤、實際 endpoint 不帶 slash + 用 `?current=`、既有 endpoint 本身不退化)+ zero-regression 1/1(C-V8 三邊 scope:base-web src 0 diff + nestjs fork 0 diff + rust-api 13 file ~305 LOC + 0 docker-compose diff + outer 4 file 1 untracked dir)= **10/10 PASS**(對齊 spec FR-021 NFR-004、無 unit test per FR-017、繼承既有 service audit hook per FR-016);**3 個 implement-time finding 文件化**:① cargo `-D warnings` 對 unused import 嚴格、`EntityTrait` 在 `sys_role_service.rs:4` 加但未 referenced 觸 docker build fail、1 LOC fix(remove import)後 build pass — code quality reviewer 預先 flagged 為 Minor、但 implementer 認為 harmless、結果證實 reviewer 對(2026-05-20 lesson:rust `-D warnings` 不 forgiving、不要 add speculative trait import)/ ② R-Q5 column choice:spec data-model E5 寫 `sys_menu::Column::Name`、實際 sys_menu entity 無 `name` column 只有 `menu_name`(display)+ `route_name`(unique 標識)、改用 `RouteName` 對齊既有 `is_route_exist` 慣例(per spec brainstorm Q5「page key for binding pages」semantics)/ ③ T028 router init pattern:spec data-model E6 寫 `merge_router!(... None, true, true, None)`、實際 `merge_router!` macro 只 inject 單 service Extension、F9 alias 跨 3 service(user + role + menu)需多 Extension layer、改用既有 `init_authorization_router` pattern 手動 `.layer(Extension(Arc::new(...))) × 3 + apply_layers(Services::None, true, true, ...)`、本質與 spec 同(JWT auth + Casbin enforce 都 enable);base-web + nestjs fork **三邊零改動**(per FR-010 + FR-011 + SC-011 + SC-012);W-FA1 stack 6 service healthy + migration exited 0 維持;rust-api image rebuild ~3-5 min warm cache hit(NFR-005 budget 內);Constitution Check 25 PASS / 7 N/A / 0 violation(Principle I RBAC fail-safe + Principle II Soft Delete + Audit 繼承既有 service hook + Principle IV 「base 不改動邊界」紀律 — RESEARCH §6.2 方案 B 後端適應 base API 預期 + Principle V 「漸進收縮」DESIGN-B 階段 F9 alias 完全繼承 identical);Brainstorm 5 顯式 Q 全拍板(Q1 getAllRoles 完整 + getAllPages 最簡 SQL / Q2 alias 路徑 + curl 驗 + base-web src 0 diff B3 留 follow-up / Q3 batchDeleteUser per-row loop + 永遠 200 + {deletedCount: N} partial counted / Q4 wrapper handler 加 entity 檔、route 集中新建 sys_system_manage_route.rs / Q5 getAllPages 回 menu 表 route_name distinct list);**Code review surface 2 個 Important defer**(spec data-model E1 直接 prescription ad-hoc `json!({"deletedCount": N})`、改 typed struct 違 spec + 增 file scope;tracing::warn 為 ops nice-to-have、stub-level 不 required、與 spec FR-017 stub 簡單性精神 tension)、Minor 全 defer(風格 nit、不影響行為);**DESIGN-A §4.2 抽離項清單交付進度 5/5 完成**:F11 4 條(sendCaptcha / verifyCaptcha / auth/error / mock/getLastTime)+ F9 1 條(batchDeleteUser)= 5/5 全交付;F9 解鎖 F13 rust-refresh-token-impl + F14 DESIGN-A→B cutover + base-web SPA manage/* 4 view 完整體驗(e2e 驗留 follow-up)、並行可選 F7 / F12 / W-F11 / W-F6b)
- [x] **F11 extracted-stubs** ✅(2026-05-19 完成;outer `6299640` + merge `81ecb0d`、rust-api `c58e619`;spec `specs/020-extracted-stubs/`;application Phase 4 收尾後**第一個** post-Phase-4 feature(F10/F10.1/F10.2 三件套全完成後接續、F9/F7/F12 之前可並行)— 補 DESIGN-A §3.1 + §4.2 抽離項清單 **4 條 stub endpoint**(`POST /auth/sendCaptcha` tracing log + 回 `{code:"000000"}` / `POST /auth/verifyCaptcha` 一律 success / `GET /auth/error` 反 echo / `GET /mock/getLastTime` 回 ISO timestamp)+ 1 個 Casbin policy seed migration(`m20260519_a_f11_extracted_stubs_seed.rs` INSERT 8 row、Soybean + Administrator allow、GeneralUser default deny);改動範圍 rust-api **13 file ~118 LOC**(10 修 + 3 新建、超 plan 預估 10 file 1 額外 Cargo.toml 加 serde_json/tracing/chrono workspace deps、per analyze G1 + G2 grounding):`server/api/src/admin/sys_authentication_api.rs` 加 3 個 stub handler / `server/api/src/admin/sys_mock_api.rs` 新建 / `server/api/src/admin/mod.rs` 加 mod + re-export / `server/api/Cargo.toml` 加 3 個 workspace dep / `server/router/src/admin/sys_authentication_route.rs` 加 3 mount + RouteInfo + `init_protected_router` Casbin enforce flip(false→true、F5.1 既有 getUserInfo 3-role seed 不退化、per implement-time deviation 文件)/ `server/router/src/admin/sys_mock_route.rs` 新建 `SysMockRouter` / `server/router/src/admin/mod.rs` 加 mod + re-export / `server/initialize/src/router_initialization.rs` 加 SysMockRouter import + register / `server/model/src/admin/input/sys_authentication.rs` 加 3 DTO + `server/model/src/admin/input/mod.rs` 加 3 DTO re-export / `migration/src/datas/m20260519_a_f11_extracted_stubs_seed.rs` 新建 + `migration/src/datas/mod.rs` register + `migration/src/lib.rs` Migrator vec 加;**兩段式 commit**(per CLAUDE.md §6.1 + F5.1/F6/F10.1/F10.2 慣例、動 rust-api worktree、**無 docker-compose.yml 改**、對比 F10.1);acceptance US1 P1 MVP 3/3(C-V1 image rebuild OK 新 SHA / C-V3 Soybean 4 endpoint 5 sub-case 全 HTTP 200 + 預期 response shape `{code:0, data, msg:"success", success:true}` + 各 stub data shape 對齊 / C-V5 tracing log `phone=13800138000` field grep ≥ 1 line)+ US2 P2 1/1(C-V4 GeneralUser sendCaptcha HTTP 200 + envelope `{code:5001, success:false, msg:"您没有访问该资源的权限"}`、per R-Q6 implement-time finding `casbin_envelope_adapter` wrap、deny 路徑生效 + 不回 `{code:"000000"}` allow data)+ US3 P3 2/2(C-V2 psql casbin_rule 8 row 落 DB、v0×v2×v3 對齊期望 / C-V7 W-FA1 stack 6 service healthy + rust-api 剛 recreated + migration init container exited 0)+ zero-regression 1/1(C-V6 三邊 scope:base-web src 0 diff + nestjs fork 0 diff + rust-api 13 file ~118 LOC + 0 docker-compose diff + outer 4 file 1 untracked dir)= **7/7 PASS**(對齊 spec FR-018 NFR-004、無 unit test、tracing log compensate per Q3);**2 個 implement-time finding 文件化為 R-Q5 + R-Q6**:① R-Q5(spec FR-005 + data-model E6 寫 `v4='allow'` 顯式、實測 Casbin model `p = sub, dom, obj, act` 4 field 下顯式 v4='allow' 與 implicit eft handling 衝突致 enforce_mut 回 Err → HTTP 502 BAD_GATEWAY;改沿用 F5.1 / F6 既有 `v4=''` baseline、business 業務語意「policy effect = allow」不變)/ ② R-Q6(spec C-V4 寫 raw HTTP 403、實測 rust-api `casbin_envelope_adapter` middleware 把 axum-casbin raw 403 wrap 成 F4 envelope `{code:5001, success:false}` HTTP 200、對齊 rust-api 全 codebase F4 envelope 紀律、acceptance pass criteria 改 envelope shape);base-web + nestjs fork **三邊零改動**(per FR-007 + FR-008 + SC-007 + SC-008);W-FA1 stack 6 service healthy + migration exited 0 維持;rust-api image rebuild ~3-5 min warm cache hit(NFR-005 budget 內);Constitution Check 17 PASS / 13 N/A / 0 violation(Principle I RBAC fail-safe + Principle V「漸進收縮」stub 為 DESIGN-A 過渡、未來升級路徑明確);Brainstorm 5 顯式 Q 全拍板(Q1 allow Soybean+Administrator / Q2 不加 menu seed / Q3 極簡 stub / Q4 acceptance Soybean×4+GU×1 / Q5 file org auth 同 sys_authentication_*、mock 新建);**1 個 implement-time deviation**:`init_protected_router` Casbin enforce flag 從 `(false, true)` flip 到 `(true, true)` 為達 spec FR-005/FR-006 + US2 要求(F11 stubs 必 Casbin enforce)、F5.1 既有 m20260515 seed 對 `/auth/getUserInfo` 3-role allow → 既有 endpoint 不退化(C-V3 prelim 驗 getUserInfo 對 Soybean 仍 HTTP 200);**DESIGN-A §4.2 抽離項清單交付進度** 4 / 5(剩 batchDeleteUser 留 F9 systemManage-alias-router 統一交付);**Application Phase 4 收尾 + 抽離項清單收尾完成**:解鎖 F9 batchDeleteUser stub 交付 → DESIGN-A §4.2 全 5/5 收;F11 後解鎖 F13 rust-refresh-token-impl + F14 DESIGN-A→B cutover、並行可選 F9 / F7 / F12 / W-F11 / W-F6b)
- [x] **F10.2 rust-tokenstatus-string-align** ✅(2026-05-19 完成;outer `d53e724` + merge `851ec79`、rust-api `cb97224`;spec `specs/019-rust-tokenstatus-string-align/`;application Phase 4 **第三個也是最後一個 feature**(F10 wire-up + F10.1 R-8 修後收尾、Application Phase 4 整套收尾、F11/F13 之前)— 修 F10/F10.1 acceptance surface 的 **R-7 friction**(nestjs `tokens.entity.ts:33` 對 rust 寫入 status=`"ACTIVE"` throw `'Token has already been used.'`):rust `TokenStatus` enum strum serialize 從 `SCREAMING_SNAKE_CASE` 改 `snake_case` + 2 個 per-variant override(`Active="unused"`、`Refreshed="used"`、`Revoked` 走 snake_case default → `"revoked"`、rust-only 狀態 per Q1 brainstorm 拍板);改 rust-api 1 file ~23 LOC(`server/constant/src/definition/consts.rs`:`serialize_all` 改 + 2 per-variant attribute + `#[cfg(test)] mod tests` 含 `test_token_status_serialize_aligns_with_nestjs` 1 fn 6 assert);**兩段式 commit**(per CLAUDE.md §6.1 + F5.1/F6/F10.1 慣例、動 rust-api worktree、無 docker-compose.yml 改、對比 F10.1);acceptance US1 P1 MVP 3/3(C-V2 login refresh_token=JWT length=289 / dots=2 F10.1 regression / C-V3 refreshToken **HTTP 201** + 新 token pair `{token, refreshToken}` R-7 修確認(nestjs framework default `POST` → 201、業務語意 = spec 寫 HTTP 200 intent、code 0 envelope success)/ C-V4 nestjs log grep `'Token has already been used'\|JsonWebTokenError\|jwt malformed`=**0 line** R-7 + R-8 全清)+ US2 P2 1/1(C-V5 psql sys_tokens 雙 row state transition、最新 status=`unused` rust 寫入對齊 nestjs + 次新 status=`used` nestjs refreshToken 用過後改、rt_len > 100 維持)+ US3 P3 1/1(C-V6 nestjs PID 1 env `REFRESH_TOKEN_SECRET=da663b30...` == `JWT_SECRET` F10.1 fallback chain regression 維持)+ rust unit test 1/1(`test_token_status_serialize_aligns_with_nestjs` 6 assert:3 forward `to_string()` + 3 reverse `from_str()` PASS)+ zero-regression 2/2(C-V7a base-web/src 0 diff + nestjs fork 0 diff + rust-api scope=1 file `consts.rs` ~23 LOC + outer 無 docker-compose.yml diff / C-V7b W-FA1 stack 6 service healthy + rust-api just recreated + migration exited 0)= **8/8 PASS**(對齊 spec FR-018 NFR-004);base-web + nestjs fork **三邊零改動**(per FR-006 + FR-009 + SC-007 + SC-008);W-FA1 stack 6 service healthy + migration exited 0 維持;rust-api image rebuild 此次 warm cache hit(極快、改 1 source file);Constitution Check 17 PASS / 13 N/A / 0 violation(Principle V「漸進收縮」DESIGN-B 階段 lowercase enum 保留、無破壞性);Brainstorm 4 顯式 Q 全拍板(Q1 Revoked rust-only / Q2 serialize_all + per-variant override / Q3 不處理舊 row / Q4 unit test SHOULD);**1 implement-time observation**:C-V3 spec 寫「HTTP 200」實際回 HTTP 201(nestjs `@Post` 預設 status、非 implementation defect、業務語意「2xx success + 新 token pair + 無 R-7 error」完全 PASS);**Application Phase 4 整套收尾完成**:F10 wire-up + F10.1 R-8 修 + F10.2 R-7 修 = refreshToken 業務流程 end-to-end pass、base-web + rust-api + nestjs fork 三邊零改動;解鎖 F11 extracted-stubs + F13 rust-refresh-token-impl(DESIGN-B 階段 rust 自驗)+ F14 DESIGN-A→B cutover)
- [x] **F10.1 rust-jwt-refresh-token-signing** ✅(2026-05-19 完成;outer `bd11d2b` + merge `48b70e6`、rust-api `211ef74`;spec `specs/018-rust-jwt-refresh-token-signing/`;application Phase 4 **第二個 feature**(F10 wire-up baseline 後接續、F10.2 之前)— rust 簽 refresh_token 從 `Ulid::new().to_string()` 改為 HS256 JWT(極簡 6-field `RefreshClaims` struct:sub/exp/iat/nbf/jti/iss)、用 W-FA1 既有 `refresh_token_secret` Docker secret + `APP_JWT_REFRESH_SECRET_FILE` _FILE pattern 載入(empty-file fallback 到 `jwt_secret` per clarify Q1 mirror nestjs entrypoint);改 8 file rust-api ~233 LOC(jwt_config.rs +2 field / secret_loader.rs +88 LOC extend apply_jwt_secret_hardening + 2 unit test / jwt.rs +126 LOC RefreshClaims + generate_refresh_token + sign_refresh_claims helper + 1 unit test / global.rs +1 REFRESH_KEYS / jwt_initialization.rs +6 REFRESH_KEYS init / sys_auth_service.rs 1 line callsite swap + 移除 unused Ulid import / application.yaml + application-test.yaml 各 +2 line)+ 改 1 outer file(`docker-compose.yml` rust-api service +1 envvar + 1 secret ref ~2 LOC);**兩段式 commit**(per CLAUDE.md §6.1 + F5.1/F6 慣例、動 rust-api worktree);acceptance US1 P1 MVP 3/3(C-V2 login refresh_token=JWT length 289 / dots=2 R-8 修 / C-V3 refreshToken HTTP 500 with `'Token has already been used.'` R-7 surface as predicted / C-V4 nestjs log `jwt malformed`=0 line + R-7 message=3 line stack trace 指 `tokens.entity.js:27:19 refreshTokenCheck`)+ US2 P2 1/1(C-V5 psql sys_tokens.refresh_token=JWT 字串 rt_len=289 / status=ACTIVE 仍 R-7 source)+ US3 P3 2/2(C-V6 rust-api env `APP_JWT_REFRESH_SECRET_FILE=/run/secrets/refresh_token_secret` + nestjs PID 1 env `REFRESH_TOKEN_SECRET=da663b30...` == `JWT_SECRET` fallback chain 對齊)+ rust unit test 3/3(`test_apply_jwt_refresh_secret_empty_file_fallback_to_jwt_secret` + `test_apply_jwt_refresh_secret_non_empty_file_uses_content` bonus + `test_generate_refresh_token_signs_valid_hs256_jwt_with_refresh_claims` 用 pure `sign_refresh_claims` helper 避 OnceCell 競爭)= **9/9 PASS**(超 spec 8/8 due to bonus non-empty file test);base-web + nestjs fork 兩邊 zero diff(per FR-006 + FR-009 + SC-008 + SC-009);W-FA1 stack 6 service healthy + migration exited 0 維持;rust-api image rebuild ~5-7 min cold;Constitution Check 17 PASS / 11 N/A / 0 violation(Principle V「漸進收縮」DESIGN-B 階段 rust 仍簽 JWT 紀律延伸);Brainstorm 1 顯式 Q + 7 自然推論 + 3 clarify Q 全拍板;**1 implement-time observation**:cargo unit test 在 rust:1.86-slim-bookworm 容器內以 host-mounted cargo cache 跑 PASS、workspace-wide `cargo test --no-run` 觸 pre-existing `axum-casbin` test 編譯錯(非 F10.1 引入、scope 外、不影響 F10.1 targeted test PASS);解鎖 F10.2 rust-tokenstatus-string-align(R-7 修點 baseline 明確、F10.1 acceptance C-V3/C-V4 已 surface `'Token has already been used.'` + `refreshTokenCheck` stack trace 證據));
- [x] **F10 refresh-token-nestjs-bridge** ✅(2026-05-19 完成;outer `4d6fc28` + merge `8f0e84c`;spec `specs/017-refresh-token-nestjs-bridge/`;application Phase 4 **第一個 feature**(Track DESIGN-A 三件套 W-FA1+W-FA2+W-FA3 deploy 結構完整後接續、F10.1/F10.2/F11/F12/F13 之前)— wire-up baseline + friction surface 紀錄 feature(post-Option A reset、0 rust patch、0 nestjs source 改動);改 / 新建 outer 純文件(`specs/017-refresh-token-nestjs-bridge/` 8 個文件 + `CLAUDE.md` SPECKIT marker + `docs/INTEGRATION-CHECKLIST.md` + `.specify/feature.json`);**單段 commit**(per W-F* / W-FA* 慣例、只動 outer 不動 worktree、不動 base-web/rust-api/nestjs 三邊);acceptance US1 P1 MVP 3/3(T020 C-V1 rust login PASS / T021 C-V2 refreshToken HTTP 500 expected fail with `jwt malformed` / T022 C-V3 nestjs log grep R-8 evidence)+ US2 P2 1/1(T030 C-V4 psql sys_tokens.status=`ACTIVE` R-7 evidence source、refresh_token = Ulid 26 char R-8 evidence)+ US3 P2 1/1(T040 quickstart 故障排查段填 friction 實測 + F10.1/F10.2 follow-up 範疇定義)= **5/5 PASS**;base-web/rust-api/nestjs fork **三邊零改動**(per FR-008 post-Option A + FR-014 + Constitution Principle IV/V);W-FA1 stack 7 service expected state 維持(6 healthy + migration exited 0);**Plan Phase 0 R-7 + R-8 finding**:R-7 rust `TokenStatus` SCREAMING_SNAKE_CASE `"ACTIVE"` vs nestjs `"unused"` 完全不對齊、R-8 rust 簽 refresh_token 用 Ulid 字串 26 char vs nestjs `jwtService.verifyAsync` 期 JWT 三段;5-9 處 rust patch 超 brainstorm Q3 1-3 處上限、觸發 R-4 abort path,**Option A 拍板 reset F10 scope** = wire-up + friction 紀錄、拆 F10.1 rust-jwt-refresh-token-signing(3 處 rust patch)+ F10.2 rust-tokenstatus-string-align(2-3 處 rust patch)兩 follow-up;**1 個 implement-time observation**:rust login response `"code":0`(integer)vs F4 spec envelope 期望 `"0000"`(string)— F4 envelope detail 偏差、非 F10 範疇、不阻 wire-up baseline、留 follow-up;Constitution Check 17 PASS / 4 N/A / 0 violation(F10 reset 強化 Principle IV/V);DESIGN-A → DESIGN-B 遷移透過 F10.1 + F10.2 + F11 + F13 + F14 完成;解鎖 F10.1 + F10.2 follow-up、application Phase 4 起點)

---

## Phase 1 P1 Roadmap（per DESIGN-A §6.1）

| # | Feature | Brainstorm | spec | plan | tasks | impl | 狀態 |
|---|---|---|---|---|---|---|---|
| F1 | `jwt-secrets` (拆 F1.1 + F1.2) | ✅ | ✅ | ✅ | ✅ | ✅（F1.1）| **完成** F1.1（F1.2 留 with F10）|
| F2 | `audit-log-infrastructure` | ✅ | ✅ | ✅ | ✅ | ✅（F2.1）| **完成** F2.1 |
| F3 | `soft-delete-infrastructure` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**（commits 上方） |
| F4 | `response-shape-alignment` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**（commits 上方） |

§6.2 規則：**P1 4 個任一順序皆可（平行 spec-kit）、但必須全部完成才能動 P2**。

**Phase 1 P1 全 4 個基礎設施 ✅ 完成（2026-05-15）— Phase 2-5 features（F5-F14）解鎖**。

---

## Phase W deploy Roadmap(per DESIGN-W §11)

| # | Feature | Brainstorm | spec | plan | tasks | impl | 狀態 |
|---|---|---|---|---|---|---|---|
| W-F1 | `dockerfile-rust-api` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(commit 上方) |
| W-F2 | `dockerfile-base-web` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(commit 上方) |
| W-F3 | `compose-base-structure` | — | ✅ | ✅ | ✅ | ✅ | **完成**(commit 上方;DESIGN-W §3 為 authoritative source、無 brainstorm 階段) |
| W-F4 | `secret-injection` | — | ✅ | ✅ | ✅ | ✅ | **完成**(commit 上方;DESIGN-W §5 + F1.1 為 authoritative source、無 brainstorm 階段) |
| W-F5 | `front-nginx` | — | ✅ | ✅ | ✅ | ✅ | **完成**(commit 上方;DESIGN-W §4 為 authoritative source、無 brainstorm 階段;**P2 第一個 feature**) |
| W-F7 | `port-mapping` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(commit 上方;dev 4 port 已落地、127.0.0.1 binding、prod baseline 仍 internal-only)|
| W-F6 | `tls-cert-management` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(commit 上方;TLS 結構 + dev 自簽 + prod 80 redirect 443 + acme.sh skeleton;cert acquisition 留 W-F6b)|
| W-F11 | `rust-horizontal-scaling` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(commit 上方;**Phase W-4 (P4) 第一個 feature** — Casbin redis pub-sub 跨 instance 一致性 + compose `replicas: 2` + nginx resolver-based upstream;observability(W-F12/W-F13/W-F14、Phase W-5 P5)為後續候選)|

§11.2 規則:**W-1 P1 4 個(W-F1 / W-F2 / W-F3 / W-F4)為部署最低基礎、必先全部完成才能動 W-2 P2(W-F5 nginx 反向代理 / W-F6 TLS / W-F7 對外 port)**。**P1 全 4 個完成、P2 解鎖**(2026-05-15);**P2 W-F5 + W-F7 + W-F6 完成**(2026-05-18)。**W-2 P2:3/3 完成(W-F5 / W-F6 / W-F7)**。W-F11 `rust-horizontal-scaling`(水平擴展能力)屬 **Phase W-4 (P4)**、非 P2(per `INTEGRATION-DESIGN-W-DEPLOYMENT.md §11` authoritative source);observability(W-F12 / W-F13 / W-F14)屬 **Phase W-5 (P5)**、為後續候選。

---

## Phase W-7 deploy Roadmap — Track DESIGN-A 三件套(per DESIGN-W §11 line 1114-1116)

| # | Feature | Brainstorm | spec | plan | tasks | impl | 狀態 |
|---|---|---|---|---|---|---|---|
| W-FA1 | `compose-nestjs-service` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(outer `b2c3ff7` + merge `b095d55`、17/17 acceptance PASS)|
| W-FA2 | `nginx-track-a-transitional-block` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(outer `e8f4dac` + merge `c5b7840`、12/12 acceptance PASS)|
| W-FA3 | `cicd-nestjs-build-job` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(outer `68c5c4e` + merge `f23f38e`、8/8 acceptance PASS)|

**W-FA1 implement-time discoveries**(2026-05-18):
- **NODE_VERSION build-arg override**:fork Dockerfile `ARG NODE_VERSION=20.11.1` 配 `pnpm@9.1.2` 觸 `ERR_UNKNOWN_BUILTIN_MODULE`(pnpm 用 Node 20.12+ 才有的 `node:sea-config`)。Spec A-002 anticipated;build cmd 加 `--build-arg NODE_VERSION=22.11.0`(LTS)解。
- **healthcheck endpoint amendment**(R-1):spec FR-008 原 `/v1/route/getConstantRoutes` 假設只觸 sys_menu 不依賴 schema 對齊;實際發現 nestjs prisma 期 `public.Status` PG enum、rust-api 用 SMALLINT 致 PG 42704 type-not-exist。**改 healthcheck 到 `/v1` root(`@Public @BypassTransform getHello()` 不查 DB)**。對齊 W-F4 R-001 amendment 模式、保證 healthcheck PASS 與 schema 對齊無關。spec FR-008 / contract C-C4 / verification C-V2/C-V7 同步調整(此次 commit 一起落)。
- **image size 870MB**:NFR-001 SHOULD ≤ 500MB 偏大(Node 22 + multi-stage 含完整 node_modules)、屬 SHOULD 非 MUST、留 W-FA3 build 優化階段精修。
- **sys_menu / sys_tokens schema 對齊**:sys_tokens 表存在(rust-api 已 migrate)、但 nestjs prisma model 用 PG enum `Status` 與 rust-api SMALLINT 分歧 — F10 範疇對齊 nestjs prisma model 到 rust-api schema(spec OOS-005)。

---

## Application Phase 4 Roadmap(per DESIGN-A §6.4 / DESIGN-W 三件套後)

| # | Feature | Brainstorm | spec | plan | tasks | impl | 狀態 |
|---|---|---|---|---|---|---|---|
| F10 | `refresh-token-nestjs-bridge` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(outer `4d6fc28` + merge `8f0e84c`、5/5 acceptance PASS、wire-up + friction baseline)|
| F10.1 | `rust-jwt-refresh-token-signing` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(outer `bd11d2b` + merge `48b70e6`、rust-api `211ef74`、9/9 acceptance PASS、R-8 修 + R-7 surface as F10.2 baseline)|
| F10.2 | `rust-tokenstatus-string-align` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(outer `d53e724` + merge `851ec79`、rust-api `cb97224`、8/8 acceptance PASS + unit test、R-7 修、Phase 4 整套收尾)|
| F11 | `extracted-stubs` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(outer `6299640` + merge `81ecb0d`、rust-api `c58e619`、7/7 acceptance PASS、4 條 stub + Casbin policy seed、DESIGN-A §4.2 抽離項 4/5 完成)|
| F9 | `systemManage-alias-router` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(outer `260f2c6` + merge `b2f910c`、rust-api `b744c8e`、10/10 acceptance PASS、10 條 alias + Casbin policy seed 20 row、**DESIGN-A §4.2 抽離項 5/5 收尾完成**)|
| F7 | `manage-crud-alignment` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(outer `44c7424` + merge `136b1eb`、rust-api `11b888c`、10/11 acceptance PASS + 1 deferred CDP smoke、5 個 Output DTO + 5 wrapper handler + Casbin policy seed 15 row、**application Phase 3 進度 2/3**、解 A-006、F7 R-Q-AT1 spec drift refinement(base path trailing slash 移除))|
| F8 | `assign-users` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(outer `f1bfff7` + merge `c2b0912`、rust-api `90f37d1`、12/12 acceptance PASS、assign_users handler + route mount + Casbin policy seed 1 row、**application Phase 3 收尾 3/3**、補齊 `/authorization/assign-*` 三件套)|
| F12 | `cleanup-job` | ✅ | ✅ | ✅ | ✅ | ✅ | **完成**(outer `78e585c` + merge `86e56e5`、rust-api `30c8dd4`、9/9 acceptance PASS、新 crate `server/cleanup` cleanup binary + docker-compose `cleanup` service(profile jobs)+ `cleanup_job` 最小權限 PG role、物理刪 7 張軟刪表過期 row + per-row txn 寫 `HARD_DELETE` audit、無 migration、**DESIGN-A 本體(F1–F12)收尾**)|

**DESIGN-A §4.2 抽離項清單交付進度**:**5 / 5 完成**(F11 4 條 sendCaptcha / verifyCaptcha / auth/error / mock/getLastTime + F9 1 條 batchDeleteUser);抽離項清單收尾、F11 + F9 兩 feature 合計補完 DESIGN-A §3.1 + §4.2 全部 stub endpoint、解鎖 F13 rust-refresh-token-impl + F14 DESIGN-A→B cutover、可並行 F12 / W-F11 / W-F6b。F9 R-Q1 spec correction(既有 `update_user` handler method-agnostic、F9 alias 直接 mount POST 不需新做 `update_user_post` wrapper)為 F11 R-Q5/R-Q6 之後第 3 個 implement-time spec drift refinement、modal pattern「acceptance 階段 catch + spec inline 修」延續。

**Application Phase 3 進度**:**3 / 3 完成**(F9 systemManage-alias-router、10 條 alias 完整交付 + F7 manage-crud-alignment、5 read alias shape 對齊 + admin path Casbin 補位 15 row + F8 assign-users、`POST /authorization/assign-users` user-role 指派 endpoint wiring + Casbin policy seed 1 row、收尾 Phase 3)。**F7 解 spec A-006 已知差異**(F9 baseline、ROLE_ADMIN 對既有 `/user/* /role/* /route/*` path Casbin 補位完整);F7 R-Q-AT1 spec drift refinement(base path trailing slash 移除、acceptance 階段 catch + 修齊 migration source + in-DB UPDATE 8 row)為 F11 R-Q5/R-Q6 + F9 R-Q1 之後第 4 個 implement-time spec correction、modal pattern「acceptance 階段 catch + spec inline 修」延續。F7 解鎖 F8 + base manage/* 4 view 跑通(精化定義為 3 view + admin path、not 4 view + UI CRUD per Q1)、並行可選 F12 / W-F11 / W-F6b。F7.2 role-code-alignment(F7.1 後 follow-up)收掉 F7.1 CDP demo 的 role alias workaround — rust `getUserInfo` 回傳 role code 套 `ROLE_*`→`R_*` alias 映射純函式、base-web static 模式 manage/* 3 view CDP smoke 不帶 workaround 原生 render;F7.2 為 F7/F7.1 base manage/* 跑通收尾 follow-up、非獨立 Phase 3 feature(F7.2 落地時進度 2/3、F8 落地後 Phase 3 收尾 3/3)。

**Plan Phase 0 R-7 / R-8 finding** 推動 F10 scope reset(Option A 拍板):
- R-7: rust `TokenStatus` SCREAMING_SNAKE_CASE(`"ACTIVE"` / `"REFRESHED"` / `"REVOKED"`)vs nestjs lowercase(`"unused"` / `"used"`)完全不對齊 — F10.2 rust-tokenstatus-string-align 解
- R-8: rust 簽 `refresh_token` 用 `Ulid::new().to_string()`(26 char 明文 Ulid)vs nestjs `jwtService.verifyAsync` 期 JWT 三段 — F10.1 rust-jwt-refresh-token-signing 解
- 加總 5-9 處 rust patch 超 brainstorm Q3 1-3 處上限、觸發 R-4 abort path、reset F10 為 wire-up + friction 紀錄 feature
- F10.1 + F10.2 完成後 nestjs refreshToken end-to-end 跑通、後續 F11 + F13 + F14 解鎖

---

## Brainstorming 決策快照

### F2.1 `audit-log-infrastructure`（進行中、brainstorm 2026-05-14 起）

> Source: `docs/superpowers/003-feature-audit-log-infrastructure.md`（brainstorm 完成後存檔）
> Parent design：DESIGN-A §1.5（全域 audit 紀律）+ §5.2.1（audit 完整性）

| # | Decision | Choice |
|---|---|---|
| 1 | F2 scope 拆分 | **F2.1 先交**：schema 擴 + transaction 紀律 + INSERT/UPDATE audit + 統一 audit path；outbox + Redis subscriber TTL fallback 留 F2.2 |
| 2 | audit 寫入路徑 | 統一為單一 audit context API（取代 HTTP middleware path + F3 `method="INTERNAL"` sentinel hack 並存）|
| 3 | sys_operation_log schema 新欄 | 4 個：`operation` enum（INSERT/UPDATE/SOFT_DELETE/RESTORE/HARD_DELETE）+ `entity_id` + `payload_before` JSONB + `payload_after` JSONB |
| 4 | INSERT/UPDATE 觸發 | Manual — service handler 顯式呼叫（同 F3 facade soft_delete 風格）|
| 5 | payload 內容格式 | Entity 完整 snapshot（`serde_json::to_value(&model)`、storage 成本 admin-heavy 場景可接受）|
| 6 | sensitive field redaction | Trait-based — `AuditSerialize::redacted_fields() -> &[&str]`、sys_user 含 `password`、sys_access_key 含 `access_key_secret` |
| 7 | Implementation approach | **Approach A**：`AuditEvent` struct + `audit_log::write_in_txn` helper（漸進演進 F3 既有 pattern）|

#### `/speckit-clarify` Session 2026-05-14 額外 3 個拍板

| # | Decision | Choice |
|---|---|---|
| 8 | HTTP middleware + service-level audit 雙寫 row 怎麼鎖？ | **Always double-write** — 同一 admin HTTP write 留 2 row（HTTP 視角 method=POST 等 + service-level method=INTERNAL）、middleware 不 dedupe |
| 9 | HTTP middleware audit row entity_type 怎麼填？ | **Hybrid rule** — URL match 7 條 admin pattern `/api/sys-(user|role|menu|domain|organization|endpoint|access-key)/*` 對應 `sys_<x>`、不 match fallback `"http_event"` |
| 10 | Migration `datas/*` seeding INSERT 是否 audit？ | **Exempt** — F2.1 audit 範圍只含 application runtime write（service + middleware）；seeding 走 git tracked migration 檔留紀錄 |

→ Spec / plan / research / data-model / contracts / quickstart / tasks 全在 `specs/003-audit-log-infrastructure/`；F2.1 已 implement 完成（10 commits in rust-api + 2 commits outer + merge to rev1-admin-root）。

### F2.1 `audit-log-infrastructure`（已完成、archived）

→ `docs/superpowers/003-feature-audit-log-infrastructure.md`

### F1.1 `jwt-secrets`（已完成、archived）

→ `docs/superpowers/004-feature-jwt-secrets.md`；spec-kit 全套 + impl 在 `specs/004-jwt-secrets/`

### F3 `soft-delete-infrastructure`（已完成、archived）

→ `docs/superpowers/002-feature-soft-delete-infrastructure.md`

### F4 `response-shape-alignment`（已完成、archived）

→ `docs/superpowers/001-feature-response-shape-alignment.md`

---

## 跨 feature 的待驗證項（Assumptions）

依 constitution §IV「上游驗證」規則 — 帶上 feature spec.md Assumptions 段、實作時驗、驗完勾掉並回填結果。

- [x] **預設密碼 = `123456`** — 舊 workspace fork260509 已驗、CLAUDE.md §5.1 已記錄；rev1 第一個 login flow feature 跑通時再次動態驗證
- [ ] **rev1 dev DB 起動 + migrations 套用 OK** — F3 G11 acceptance test 需此；deploy/ stack 起來才能驗
- [ ] **F3 quickstart Step 7 端到端**（curl login + admin soft-delete + 8888 envelope）— 需 real postgres + redis + rust-api server 起；目前 deploy/ 尚未建立
- [ ] **F3 acceptance tests 跑通**（`cargo test --test soft_delete_basics -- --ignored` × 3 test files、共 9 個 `#[ignore]` test fn）— 需 export `TEST_DATABASE_URL` + migration up
- [ ] **CI lint workflow 在實際 PR 觸發 + block merge** — `.github/workflows/ci-soft-delete-lint.yml` 已建、待第一個 PR 觸發驗證

---

## Deferred / future backlog

### F3 完成後留下的 follow-up

| ID | 範疇 | 處理 | 備註 |
|---|---|---|---|
| F3-N1 | `sys_endpoint::insert_many` fully-qualified | F2.1 評估 | 服務內 `server_model::admin::entities::sys_endpoint::Entity::insert_many(...)` 繞 facade、註解明示合規（facade 只封 SELECT/DELETE）；若 F2.1 audit path 要納 INSERT，facade 補 `insert_many` wrapper 是自然動作 |
| F3-N2 | `sys_access_key` delete atomicity gap | F2.2 / F12 評估 | facade commit → `sign::remove_key` validator 兩行間 process crash 留 validator orphan key；改 DB-as-truth + initialize 重 reload pattern；F2.2 outbox 模式或 F12 cleanup-job 階段處理 |
| F3-N3 | `sys_endpoint::batch_remove_endpoints` partial-failure | F2.1 / F2.2 | 改 log-and-continue 後失去 atomicity；F2.2 outbox 模式可重整 |
| F3-N4 | pre-existing `print!("user is {:#?}", user)` debug 痕 | 任一後續 feature 順手清 | F3 G6 review 發現既有 pre-F3 code、F3 沒清；3 處：sys_user_api / sys_menu_api / sys_authorization_service |
| F3-N5 | `data-model.md §E4` 範例 path 與實際 impl 位置 drift | spec hygiene | spec §E4 範例假設 impls 在 server-core、實際因循環 dep 落在 server-model/src/admin/soft_delete_impls.rs（per analyse C3 precedent）；建議補 errata 一行 |
| F3-N6 | F2 audit-log schema 升級後 F3 helper 對齊 | F2.1 內處理 | F3 FR-022 已預告「F2 升級 sys_operation_log schema 時、F3 callsite 不需動」— F2.1 refactor write_in_txn 時順帶完成 |

---

## 維護指引

每次 feature 推進後，**在同一個 commit 內**更新本檔：

| 階段 | 改 roadmap 表的哪欄 |
|---|---|
| brainstorm 完成（`docs/superpowers/<NNN>-feature.md` 寫定）| Brainstorm 欄改 ✅ + 決策快照寫入本檔對應 section |
| `/speckit-specify` 完成 | spec 欄改 ✅ |
| `/speckit-plan` 完成 | plan 欄改 ✅ |
| `/speckit-tasks` 完成 | tasks 欄改 ✅ |
| 實作 merge 回 default | impl 欄改 ✅、狀態欄改「完成」、加 outer + worktree commit SHA |
| 上游驗證項 done | 把對應勾選改 ✅、結果回填 CLAUDE.md（操作事實）或 spec.md（feature-specific）|

驗證證據（cargo test log、curl 輸出、psql 結果）放在 spec.md / plan.md / quickstart.md、本檔僅勾選與簡述。
