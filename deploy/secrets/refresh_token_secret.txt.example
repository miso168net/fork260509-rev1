# Refresh token signing secret for rust-api (consumed via APP_JWT_REFRESH_SECRET_FILE)
# Generate via: openssl rand -hex 32
# Or leave file empty / missing → rust-api falls back to JWT_SECRET (per F10.1 secret_loader refresh_secret hardening)
REPLACE_WITH_64_HEX_CHARS_OR_LEAVE_EMPTY_FOR_FALLBACK
